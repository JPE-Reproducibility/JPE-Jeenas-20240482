## Appendix: Detailed PII Detection Results

*Generated on 2026-06-08 14:41:07*

This appendix lists all detected instances of potential personally identifiable information (PII) in the project files. Each entry shows the matched PII terms and, for data files, sample values to help verify whether the flagged content is indeed sensitive.

### Data Files

**/replication-package/JPE_REPLICATION_FINAL/LICENSE.txt**

- Variable: `to any person obtaining a copy`
  - Matched terms: son
  - Sample values: 

**/replication-package/JPE_REPLICATION_FINAL/data/raw/FRB_Z1.csv**

- Variable: `Domestic financial sectors; long-term debt securities; asset`
  - Matched terms: lon
  - Sample values: Currency, 1000000, USD
- Variable: `Domestic financial sectors; long-term debt securities; liability`
  - Matched terms: lon
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; IRAs; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; Sallie Mae loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; Treasury securities, excluding U.S. savings securities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; Treasury securities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; U.S. government loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; agency- and GSE-backed securities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; assets in 529 plans`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; assets in 529 pre-paid plans`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; assets in 529 savings plans`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; cash accounts at brokers and dealers`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; checkable deposits and currency; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; commercial mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; compensation of employees received`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; consumer credit, automobile loans; liability `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; consumer credit, student loans; liability `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; consumer credit; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; consumer credit; liability_1`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; consumer durable goods, current cost basis`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; corporate and foreign bonds; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; corporate equities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; debt securities and loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; debt securities and loans; liability_1`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; debt securities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; depository institution loans n.e.c.; liability `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; directly and indirectly held corporate equities as a percentage of total assets`
  - Matched terms: house
  - Sample values: Percent, 1, NA
- Variable: `Households and nonprofit organizations; directly and indirectly held corporate equities as a percentage of total financial assets`
  - Matched terms: house
  - Sample values: Percent, 1, NA
- Variable: `Households and nonprofit organizations; directly and indirectly held corporate equities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; entitlements to non-pension benefits; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; equity and investment fund shares; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; equity investment in funding corporations under PPIP; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; farm mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; funded pension entitlements; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; holding gains on assets at current cost`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; holding gains on assets at market value`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; home mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; home mortgages; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; home mortgages; liability_1`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; indirectly held corporate equities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; insurance receivables due from property-casualty insurance companies; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; insurance, pension and standardized guarantee schemes; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; life insurance reserves excluding unit-linked; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; life insurance reserves; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; loans, excluding mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; loans; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; long-term debt securities; asset`
  - Matched terms: house, lon
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; money market mutual fund shares; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; multifamily residential mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; municipal securities; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; mutual fund and money market fund shares; asset `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; mutual fund shares, bond funds; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; mutual fund shares, equity funds; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; mutual fund shares, other than bond or equity funds; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; mutual fund shares; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; net equity in life insurance and pension funds; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; net lending (+) or borrowing (-) (financial account)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; net worth `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; net worth as a percentage of disposable personal income`
  - Matched terms: house, son
  - Sample values: Percent, 1, NA
- Variable: `Households and nonprofit organizations; non-revolving consumer credit; liability `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; nonfinancial assets`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other deposits including time and savings deposits; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other equity; asset (IMA) `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other financial assets (FSIs)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other financial assets, excluding directly and indirectly held equity`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other liabilities (FSIs)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other loans and advances; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other loans and advances; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; other non-revolving consumer credit; liability `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; pension entitlements; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; policy loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; private foreign deposits; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; proprietors' equity in noncorporate business`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; real estate at market value`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; residential structures, current cost basis`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; retirement assets`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; retirement entitlements; asset (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; revolving consumer credit; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; short-term loans excluding consumer credit; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; short-term loans; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; syndicated loans to nonfinancial corporate business; asset `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total assets`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total currency and deposits including money market fund shares; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total currency and deposits; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total financial assets `
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total liabilities`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total liabilities and net worth (IMA)`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total miscellaneous assets`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total mortgages; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total mortgages; liability`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; total time and savings deposits; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households and nonprofit organizations; unit-linked life insurance reserves; asset`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households; owner-occupied real estate including vacant land and mobile homes at market value`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households; owners' equity in real estate`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Households; owners' equity in real estate as a percentage of household real estate`
  - Matched terms: house
  - Sample values: Percent, 1, NA
- Variable: `Households; residential structures, current cost basis`
  - Matched terms: house
  - Sample values: Currency, 1000000, USD
- Variable: `Nonfinancial business; long-term debt securities; asset`
  - Matched terms: lon
  - Sample values: Currency, 1000000, USD

**/replication-package/JPE_REPLICATION_FINAL/data/raw/bds2018.csv**

- Variable: `job_creation_births`
  - Matched terms: birth
  - Sample values: 5186329, 4846585, 4636595
- Variable: `job_creation_rate_births`
  - Matched terms: birth
  - Sample values: 7.77, 6.805, 6.303
- Variable: `reallocation_rate`
  - Matched terms: loc, location
  - Sample values: 28.408, 27.092, 31.791

**/replication-package/JPE_REPLICATION_FINAL/data/raw/bds2018_fage.csv**

- Variable: `job_creation_births`
  - Matched terms: birth
  - Sample values: 2582124, 2429409, 2239357
- Variable: `job_creation_rate_births`
  - Matched terms: birth
  - Sample values: 199.998, 199.999, 200.000
- Variable: `reallocation_rate`
  - Matched terms: loc, location
  - Sample values: 0, 65.553, 63.739

**/replication-package/JPE_REPLICATION_FINAL/proprietary-data-not-for-publication/raw/Wscope_data_raw.xlsx**

- Variable: `NAME`
  - Matched terms: name
  - Sample values: ABR INFO.SVS. DEAD - MERGER 22/07/99, ACCOM DEAD - DELIST.11/05/17, ACLARA BIOSCIENCES DEAD - DELIST.13/12/04

### Code Files

**/replication-package/JPE_REPLICATION_FINAL/Master_main.py**

- Line 8: name
  ```
  except NameError:
  ```
- Line 65: loc, lon
  ```
  # Estimate empirical aggregate IRFs with local projections, and plot alongside model IRFs (Appendix 
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Julia/Julia_empirics_code/BDS_analysis_code.jl**

- Line 40: lat
  ```
  write(joinpath(PROJECT_ROOT, "output", "tables", "_partof_TabB1_BDS_calibtarget.txt"), "Relative siz
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Julia/Julia_model_code/Master_model_work.jl**

- Line 19: block, loc
  ```
  ### Calibration/parameter setting block
  ```
- Line 22: lat
  ```
  Pi_set      = 1.02^(1.0/4.0)         # Gross inflation
  ```
- Line 33: lon
  ```
  gamma_set   = 1-1/16    # Long-term debt decay parameter
  ```
- Line 34: house
  ```
  varpi_set   = 1.0       # Household's CRRA parameter
  ```
- Line 35: house
  ```
  psi_set     = 1.2       # Place-holder for household's labor disutility (overwritten in w_ss=1 norma
  ```
- Line 40: city
  ```
  # Capital price elasticity to I/K
  ```
- Line 72: block, loc
  ```
  ### End calibration block
  ```
- Line 79: lon
  ```
  b_min_set, b_max_set = -theta_set, 0.0      # Bounds on firms' space b which in the code refers to t
  ```
- Line 135: name
  ```
  # Give the model a name
  ```
- Line 136: lname, name
  ```
  modlname = "mod1"
  ```
- Line 143: lname, name
  ```
  txtout_path = joinpath(PROJECT_ROOT, "interim_output", "model_interim_output", "savedout", modlname 
  ```
- Line 150: lname, name
  ```
  modl = Model(beta=beta_set, psi=psi_set, varpi=varpi_set, rhoz=rhoz_set, sige=sige_set, A=1.0, alpha
  ```
- Line 200: lat
  ```
  # Characterize age groups in population
  ```
- Line 254: lat
  ```
  Ba_prob_itp  = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.Ba_pr
  ```
- Line 255: lat
  ```
  kpA_itp     = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.kpA, G
  ```
- Line 256: lat
  ```
  bpA_itp     = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.bpA, G
  ```
- Line 280: name
  ```
  savefig(joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig4a_"*modl.name*"_polHM_Ba_i
  ```
- Line 314: lat
  ```
  # Simulate MChains
  ```
- Line 316: lat
  ```
  zi_col[ii,:]   = simulate(mcz,T,init=zi_col_init[ii]);
  ```
- Line 319: lat
  ```
  # Simulate paths in IRF and SS
  ```
- Line 327: lat
  ```
  kpA_itp = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.kpA, Gridd
  ```
- Line 328: lat
  ```
  bpA_itp = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.bpA, Gridd
  ```
- Line 335: lat
  ```
  # True level of b in the simulated paths
  ```
- Line 342: loc
  ```
  # Characterize untargeted moments locally
  ```
- Line 348: lat
  ```
  # Correlation of m/a and log size
  ```
- Line 356: lat
  ```
  # Correlation of m/a and i/k
  ```
- Line 360: lat
  ```
  # Simple calculations for theory Appendix B.5.3
  ```
- Line 431: name
  ```
  Plots.savefig(joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig4b_"*modl.name*"_tdis
  ```
- Line 438: lat, lon
  ```
  # Simulate another, longer sample and save sim_paths_SS into a csv of a balanced panel of firm-level
  ```
- Line 440: lon
  ```
  Nfirms_long=20000;
  ```
- Line 441: lon
  ```
  T_long=40;
  ```
- Line 442: lon
  ```
  zi_col_long, xi_cdf_col_long =  Array{Int64,2}(undef, Nfirms_long,T_long), zeros(Nfirms_long, T_long
  ```
- Line 446: lon
  ```
  ii_samp_long = wsample(1:sspace.Nsf, Lin_arg, Nfirms_long);
  ```
- Line 447: lon
  ```
  se_init_long = sspace.sf[ii_samp_long, 1:3];
  ```
- Line 448: lon
  ```
  xi_cdf_col_long = rand(Nfirms_long,T_long);
  ```
- Line 449: lon
  ```
  zi_col_init_long= repeat(1:sspace.nf[4], inner=sspace.nf[1]*sspace.nf[2]*sspace.nf[3])[ii_samp_long]
  ```
- Line 450: lat
  ```
  # Simulate MChains
  ```
- Line 451: lon
  ```
  for ii in 1:Nfirms_long
  ```
- Line 452: lat, lon
  ```
  zi_col_long[ii,:]   = simulate(mcz,T_long,init=zi_col_init_long[ii]);
  ```
- Line 454: lon
  ```
  sim_paths_SS_long  = sim_firm_SS(se_init_long, zi_col_long, xi_cdf_col_long, fsoln, true, modl, sspa
  ```
- Line 457: lon
  ```
  V0e_paths_SS_long = zeros(size(sim_paths_SS_long.k_path));
  ```
- Line 459: lat
  ```
  V0e_itp = interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), fsoln.V0e, Gridd
  ```
- Line 460: lon
  ```
  for tt=1:T_long
  ```
- Line 461: lon
  ```
  for ii=1:Nfirms_long
  ```
- Line 462: lon
  ```
  V0e_paths_SS_long[ii,tt] = V0e_itp(sim_paths_SS_long.k_path[ii,tt], sim_paths_SS_long.m_path[ii,tt],
  ```
- Line 465: lat
  ```
  # True level of b in the simulated paths
  ```
- Line 466: lon
  ```
  b_tr_paths_SS_long = sim_paths_SS_long.k_path.*sim_paths_SS_long.b_path/ Rc_ss;
  ```
- Line 467: lon
  ```
  Tobinq_paths_SS_long = (V0e_paths_SS_long - ((1.0+modl.rb)/modl.Pi)*b_tr_paths_SS_long) ./ (pQ_ss*si
  ```
- Line 469: lon
  ```
  df_sim_paths = DataFrame(gvkey=vec(repeat(1:Nfirms_long,1,T_long)'), cqtr_num=vec(repeat((1:T_long)'
  ```
- Line 520: loc
  ```
  local MU_in, pQ_in
  ```
- Line 541: lat
  ```
  opts.phi_pQ_acl = 0.5       # Further dampen updating of pQ path relative to MU path
  ```
- Line 570: loc
  ```
  local manl_ftsize = 13
  ```
- Line 571: lat
  ```
  fg = Plots.plot(0:Tmax, [4*100*(airf_g_base.rf_col[1:Tmax+1].-modl.rf), 4*100*(airf_g_base.Pi_col[1:
  ```
- Line 579: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig5_"*modl.name*"IR
  ```
- Line 586: lat
  ```
  # Given the baseline solution, draw random set of firms, simulate panel in SS and IRF and compute/pl
  ```
- Line 603: loc
  ```
  local manl_ftsize = 13
  ```
- Line 604: lat
  ```
  fg = Plots.plot(0:Tmax, [4*100*(airf_g_base_phiw0.rf_col[1:Tmax+1].-modl_base_phiw0.rf), 4*100*(airf
  ```
- Line 612: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB10_"*modl.name*"
  ```
- Line 615: lat
  ```
  # Given the baseline solution, draw random set of firms, simulate panel in SS and IRF and compute/pl
  ```
- Line 730: lat
  ```
  fg = Plots.plot(0:Tmax, [4*100*(airf_g_hr_cf.rf_col[1:Tmax+1].-modl_hr_cf.rf), 4*100*(airf_g_hr_cf.P
  ```
- Line 738: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB7_"*modl_hr_cf.n
  ```
- Line 775: loc
  ```
  opts.tol_p_col = 2*1e5 # To ensure no iterations on price paths, locally
  ```
- Line 786: second
  ```
  # Second, the PE effect of going to the Post-90 economy, but imposing Pre-90 price IRF paths
  ```
- Line 792: loc
  ```
  opts.tol_p_col = 2*1e5 # To ensure no iterations on price paths, locally
  ```
- Line 816: lat
  ```
  # Compute population liquidity ratio percentiles
  ```
- Line 851: lat
  ```
  {\\footnotesize \\emph{Notes:} \$q_{\\ell}^{\\alpha}\$ is the \$\\alpha\$-th percentile of the firms
  ```
- Line 853: lon
  ```
  (log) response of total capital one year after \$\\varepsilon_{0}^{f}\$ for firms with \$\\ell_{i,0}
  ```
- Line 867: lon
  ```
  # After this, irfsoln_g_hr_cf no longer needed
  ```
- Line 882: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 890: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 898: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 906: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 923: lat
  ```
  # Given the baseline solution, draw random set of firms, simulate panel in SS and IRF and compute re
  ```
- Line 930: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 938: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 946: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 956: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 964: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 972: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 980: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 996: lat
  ```
  # Given the baseline solution, draw random set of firms, simulate panel in SS and IRF and compute re
  ```
- Line 1003: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1013: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1021: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1031: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1039: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1047: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1088: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB5_"*modl.name*"_
  ```
- Line 1096: lat, son
  ```
  # Plot comparison base model (capital accumulation)
  ```
- Line 1104: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig7b_"*modl.name*"_
  ```
- Line 1107: lat
  ```
  # Plot decomposition of channels (to capital accumulation) in aggregate
  ```
- Line 1117: lat, son
  ```
  # Plot comparison base model (capital accumulation) -- Figure 7a
  ```
- Line 1123: lat
  ```
  fg=Plots.plot!(0:(T_plot-1), 100*log.(airf_g_base_decomp_M.Ka_col[1:T_plot]./aeq_base.Ka), label="\$
  ```
- Line 1127: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig7a_"*modl.name*"_
  ```
- Line 1137: lat, son
  ```
  # Plot comparison base model (capital accumulation) -- Figure B.8
  ```
- Line 1146: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB8_"*modl.name*"_
  ```
- Line 1161: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig8a_"*modl.name*"_
  ```
- Line 1164: son
  ```
  # Plot comparison -- Figure 8b
  ```
- Line 1171: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "Fig8b_"*modl.name*"_
  ```
- Line 1173: lat
  ```
  # Compute responses relative to hr_cf
  ```
- Line 1177: lat
  ```
  # Finally, compare the rmtaureal to relative importance of rbtaureal
  ```
- Line 1194: lat
  ```
  # Compute the relative role of the different channels in explaining the (post-1990) gap, as reported
  ```
- Line 1216: lat
  ```
  # Compute the size of increase in Ma, relative to initial NW (as expressed in footnote 57)
  ```
- Line 1225: lat
  ```
  # Plot decomposition of channels (to capital accumulation) in base_phiw0
  ```
- Line 1235: lat, son
  ```
  # Plot comparison base model (capital accumulation) -- Figure B.11b
  ```
- Line 1241: lat
  ```
  fg=Plots.plot!(0:(T_plot-1), 100*log.(airf_g_base_phiw0_decomp_M.Ka_col[1:T_plot]./aeq_base_phiw0.Ka
  ```
- Line 1245: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB11b_"*modl.name*
  ```
- Line 1266: lat
  ```
  # Autocorrelation of shock of 0.61, like for baseline monetary shock:
  ```
- Line 1276: loc
  ```
  local MU_in, pQ_in
  ```
- Line 1318: son
  ```
  # Construct comparison of one specific variable across the three relevant models (in GE) -- Figure B
  ```
- Line 1330: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB9_"*modl.name*"_
  ```
- Line 1352: loc
  ```
  local MU_in, pQ_in
  ```
- Line 1378: city
  ```
  # Overwrite capital price elasticity parameters
  ```
- Line 1423: lat
  ```
  manprintln("Running hr_cf_basephimresp_noGE IRF calculation...", txtout_path)
  ```
- Line 1432: lat
  ```
  manprintln("Running base_hr_cf_basephimresp_noGE IRF calculation...", txtout_path)
  ```
- Line 1449: lat
  ```
  fg = Plots.plot(0:Tmax, [4*100*(airf_g_base.rf_col[1:Tmax+1].-modl.rf), 4*100*(airf_g_base.Pi_col[1:
  ```
- Line 1457: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB12_"*modl.name*"
  ```
- Line 1460: lat
  ```
  # Given the baseline solution, draw random set of firms, simulate panel in SS and IRF and compute/pl
  ```
- Line 1474: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB14a_"*modl.name*
  ```
- Line 1483: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1491: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1499: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1507: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1517: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1525: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1535: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1543: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1553: lat
  ```
  # And forget in order to save RAM, to be loaded as needed later
  ```
- Line 1557: lat
  ```
  # Plot decomposition of channels (to capital accumulation) in aggregate
  ```
- Line 1565: lat, son
  ```
  # Plot comparison base model (capital accumulation) -- Figure B.13b
  ```
- Line 1574: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB13b_"*modl.name*
  ```
- Line 1583: son
  ```
  # Plot comparison -- Figure B.14b
  ```
- Line 1590: name
  ```
  Plots.savefig(fg, joinpath(PROJECT_ROOT, "output", "figures", "model_figures", "FigB14b_"*modl.name*
  ```
- Line 1592: lat
  ```
  # Compute responses relative to hr_cf
  ```
- Line 1597: lat
  ```
  # Recompute the relative role of the Q channel in explaining the (post-1990) gap, as reported in las
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Julia/Julia_model_code/auxiliary_model_functions.jl**

- Line 21: name
  ```
  read_tmp = read(filename, String)
  ```
- Line 22: name
  ```
  write(filename, read_tmp * s * "\n")
  ```
- Line 62: lat
  ```
  phi_pQ_acl::Float64     # Acceleration weight on updating path for pQ relative to updating MU (if <1
  ```
- Line 64: lat
  ```
  # Related to Sobol calibration
  ```
- Line 166: house, lat
  ```
  varpi::Float64          # Coefficient of household's relative risk aversion
  ```
- Line 177: lat
  ```
  Pi::Float64             # Steady state GROSS inflation rate
  ```
- Line 191: city
  ```
  phi_k::Float64          # Capital price elasticity
  ```
- Line 200: name
  ```
  name::String            # Model name for saving files
  ```
- Line 225: lat
  ```
  # Firms' responses/simulations container
  ```
- Line 268: lat
  ```
  Pi_col::Array{Float64,1}        # Path of gross inflation
  ```
- Line 300: lat
  ```
  Pi_col::Array{Float64,1}    # Path for gross inflation
  ```
- Line 335: name
  ```
  modl = Model(beta, psi, varpi, rhoz, sige, A, alpha, nu, delta, phi_m*(Pi/beta - 1), Pi/beta - 1, bs
  ```
- Line 356: lon
  ```
  Pz[:,end]   = 1.0 .- sum(Pz[:,1:end-1],dims=2);  # Ensure sum to 1, up to machine epsilon.
  ```
- Line 534: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 635: second
  ```
  # 1ai   - Second stage of N
  ```
- Line 643: lat
  ```
  kpN2_itp = extrapolate(interpolate((sspace.k_grid, sspace.mtil_grid, sspace.b_grid, sspace.z_grid), 
  ```
- Line 644: lat
  ```
  mpN2_itp = extrapolate(interpolate((sspace.k_grid, sspace.mtil_grid, sspace.b_grid, sspace.z_grid), 
  ```
- Line 645: lat
  ```
  bpN2_itp = extrapolate(interpolate((sspace.k_grid, sspace.mtil_grid, sspace.b_grid, sspace.z_grid), 
  ```
- Line 661: second
  ```
  # Solve second stage of N
  ```
- Line 665: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 741: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.m_grid), V0e_tp1[:,:,ibp,iz], Gri
  ```
- Line 754: lat
  ```
  # Having established which elements of the k'- and k-vectors to keep, construct them here to use for
  ```
- Line 761: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), sspace.k_grid[i_kN_NC_val], Gr
  ```
- Line 762: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), M_tp1*V0em_tp1[i_kN_NC_val,imp
  ```
- Line 764: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), V0eb_tp1[i_kN_NC_val,imp,ibp,i
  ```
- Line 765: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), V0e_tp1[i_kN_NC_val,imp,ibp,iz
  ```
- Line 766: lat
  ```
  V0ebdk_uc_itp   = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), V0eb_tp1[i_kN_NC_val,imp,ibp,i
  ```
- Line 769: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((kN_uc_val, ), sspace.k_grid, Gridded(Linear())), Line())
  ```
- Line 770: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((kN_uc_val, ), M_tp1*V0em_tp1[:,imp,ibp,iz], Gridded(Linea
  ```
- Line 772: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((kN_uc_val, ), V0eb_tp1[:,imp,ibp,iz], Gridded(Linear())),
  ```
- Line 773: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((kN_uc_val, ), V0e_tp1[:,imp,ibp,iz], Gridded(Linear())), 
  ```
- Line 774: lat
  ```
  V0ebdk_uc_itp   = extrapolate(interpolate((kN_uc_val, ), V0eb_tp1[:,imp,ibp,iz]./sspace.k_grid, Grid
  ```
- Line 797: lat
  ```
  # On the mtil points "spanned" by mtilN_uc_val, do the usual inversion, but NOT extrapolating below
  ```
- Line 847: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.m_grid), V0e_tp1[:,:,ibp,iz], Gri
  ```
- Line 861: lat
  ```
  # Having established which elements of the m'- and mtil-vectors to keep, construct them here to use 
  ```
- Line 868: lat
  ```
  mpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), sspace.m_grid[i_mtilN_NC
  ```
- Line 869: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), kpN_sol_k_temp[ik,i_mtil
  ```
- Line 870: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), lambda2N_sol_k_temp[ik,i
  ```
- Line 872: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), V0eb_sol_k_temp[ik,i_mti
  ```
- Line 873: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), V0e_sol_k_temp[ik,i_mtil
  ```
- Line 874: lat
  ```
  V0ebdk_uc_itp   = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), V0ebdk_sol_k_temp[ik,i_m
  ```
- Line 877: lat
  ```
  mpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), sspace.m_grid, Gridded(Linear())), Line(
  ```
- Line 878: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), kpN_sol_k_temp[ik,:], Gridded(Linear()))
  ```
- Line 879: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((mtilN_uc_val, ), lambda2N_sol_k_temp[ik,:], Gridded(Linea
  ```
- Line 881: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((mtilN_uc_val, ), V0eb_sol_k_temp[ik,:], Gridded(Linear())
  ```
- Line 882: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), V0e_sol_k_temp[ik,:], Gridded(Linear()))
  ```
- Line 883: lat
  ```
  V0ebdk_uc_itp   = extrapolate(interpolate((mtilN_uc_val, ), V0ebdk_sol_k_temp[ik,:], Gridded(Linear(
  ```
- Line 890: lat
  ```
  # Allow for extrapolation in mtil space above the upper bound, but not below
  ```
- Line 919: lat
  ```
  V0ek_c_itp     = extrapolate(interpolate((sspace.k_grid, ), V0ek_tp1[:,1,ibp,iz], Gridded(Linear()))
  ```
- Line 920: lat
  ```
  V0em_c_itp     = extrapolate(interpolate((sspace.k_grid, ), V0em_tp1[:,1,ibp,iz], Gridded(Linear()))
  ```
- Line 921: lat
  ```
  V0eb_c_itp     = extrapolate(interpolate((sspace.k_grid, ), V0eb_tp1[:,1,ibp,iz], Gridded(Linear()))
  ```
- Line 922: lat
  ```
  V0e_c_itp      = extrapolate(interpolate((sspace.k_grid, ), V0e_tp1[:,1,ibp,iz], Gridded(Linear())),
  ```
- Line 923: lat
  ```
  # The object to interpolate is V0ebdk_c_itp=V0eb/k'
  ```
- Line 924: lat
  ```
  V0ebdk_c_itp   = extrapolate(interpolate((sspace.k_grid, ), V0eb_tp1[:,1,ibp,iz]./sspace.k_grid, Gri
  ```
- Line 1028: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.m_grid), V0e_tp1[:,:,1,iz], Gridd
  ```
- Line 1041: lat
  ```
  # Having established which elements of the k'- and k-vectors to keep, construct them here to use for
  ```
- Line 1048: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), sspace.k_grid[i_kN_NC_val], Gr
  ```
- Line 1049: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), M_tp1*V0em_tp1[i_kN_NC_val,imp
  ```
- Line 1051: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), V0eb_tp1[i_kN_NC_val,imp,1,iz]
  ```
- Line 1052: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((kN_uc_val[i_kN_NC_val], ), V0e_tp1[i_kN_NC_val,imp,1,iz],
  ```
- Line 1055: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((kN_uc_val, ), sspace.k_grid, Gridded(Linear())), Line())
  ```
- Line 1056: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((kN_uc_val, ), M_tp1*V0em_tp1[:,imp,1,iz], Gridded(Linear(
  ```
- Line 1058: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((kN_uc_val, ), V0eb_tp1[:,imp,1,iz], Gridded(Linear())), L
  ```
- Line 1059: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((kN_uc_val, ), V0e_tp1[:,imp,1,iz], Gridded(Linear())), Li
  ```
- Line 1126: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.m_grid), V0e_tp1[:,:,1,iz], Gridd
  ```
- Line 1140: lat
  ```
  # Having established which elements of the m'- and mtil-vectors to keep, construct them here to use 
  ```
- Line 1147: lat
  ```
  mpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), sspace.m_grid[i_mtilN_NC
  ```
- Line 1148: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), kpN_sol_k_temp[ik,i_mtil
  ```
- Line 1149: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), lambda2N_sol_k_temp[ik,i
  ```
- Line 1151: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), V0eb_sol_k_temp[ik,i_mti
  ```
- Line 1152: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((mtilN_uc_val[i_mtilN_NC_val], ), V0e_sol_k_temp[ik,i_mtil
  ```
- Line 1155: lat
  ```
  mpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), sspace.m_grid, Gridded(Linear())), Line(
  ```
- Line 1156: lat
  ```
  kpN_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), kpN_sol_k_temp[ik,:], Gridded(Linear()))
  ```
- Line 1157: lat
  ```
  lambda2N_uc_itp = extrapolate(interpolate((mtilN_uc_val, ), lambda2N_sol_k_temp[ik,:], Gridded(Linea
  ```
- Line 1159: lat
  ```
  V0eb_uc_itp     = extrapolate(interpolate((mtilN_uc_val, ), V0eb_sol_k_temp[ik,:], Gridded(Linear())
  ```
- Line 1160: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((mtilN_uc_val, ), V0e_sol_k_temp[ik,:], Gridded(Linear()))
  ```
- Line 1204: lat
  ```
  # To avoid issues implied by the extrapolation in the k-dimension associated with states mpN_sol_muk
  ```
- Line 1207: lat
  ```
  # Such values may lead to some extreme behavior in the k' policy, mainly due to a "near constrained"
  ```
- Line 1254: lat
  ```
  # Use the relation between b' and k'/k and b' to infer the implied k'/k and k'
  ```
- Line 1263: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid), V0e_tp1[:
  ```
- Line 1276: lat
  ```
  # Having established which elements of the b'- and b-vectors to keep, construct them here to use for
  ```
- Line 1283: lat
  ```
  bpN_UC_itp = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), sspace.b_grid[i_b_NC_val], Gridded(Li
  ```
- Line 1284: lat
  ```
  kpN_UC_itp = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), kpN_sol_bp[iik,iimtil,i_b_NC_val], Gr
  ```
- Line 1285: lat
  ```
  mpN_UC_itp = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), mpN_sol_bp[iik,iimtil,i_b_NC_val], Gr
  ```
- Line 1286: lat
  ```
  lambda2N_UC_itp = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), lambda2N_sol_bp[iik,iimtil,i_b_N
  ```
- Line 1287: lat
  ```
  V2Nk_UC_itp    = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), V2Nk_sol_bp[iik,iimtil,i_b_NC_val
  ```
- Line 1288: lat
  ```
  V2Nmtil_UC_itp = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), V2Nmtil_sol_bp[iik,iimtil,i_b_NC_
  ```
- Line 1289: lat
  ```
  V2Nb_UC_itp    = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), V2Nb_sol_bp[iik,iimtil,i_b_NC_val
  ```
- Line 1290: lat
  ```
  V2N_UC_itp     = extrapolate(interpolate((b_UC_val[i_b_NC_val], ), V2N_sol_bp[iik,iimtil,i_b_NC_val]
  ```
- Line 1293: lat
  ```
  bpN_UC_itp = extrapolate(interpolate((b_UC_val, ), sspace.b_grid, Gridded(Linear())), NaN)
  ```
- Line 1294: lat
  ```
  kpN_UC_itp = extrapolate(interpolate((b_UC_val, ), kpN_sol_bp[iik,iimtil,:], Gridded(Linear())), NaN
  ```
- Line 1295: lat
  ```
  mpN_UC_itp = extrapolate(interpolate((b_UC_val, ), mpN_sol_bp[iik,iimtil,:], Gridded(Linear())), NaN
  ```
- Line 1296: lat
  ```
  lambda2N_UC_itp = extrapolate(interpolate((b_UC_val, ), lambda2N_sol_bp[iik,iimtil,:], Gridded(Linea
  ```
- Line 1297: lat
  ```
  V2Nk_UC_itp    = extrapolate(interpolate((b_UC_val, ), V2Nk_sol_bp[iik,iimtil,:], Gridded(Linear()))
  ```
- Line 1298: lat
  ```
  V2Nmtil_UC_itp = extrapolate(interpolate((b_UC_val, ), V2Nmtil_sol_bp[iik,iimtil,:], Gridded(Linear(
  ```
- Line 1299: lat
  ```
  V2Nb_UC_itp    = extrapolate(interpolate((b_UC_val, ), V2Nb_sol_bp[iik,iimtil,:], Gridded(Linear()))
  ```
- Line 1300: lat
  ```
  V2N_UC_itp     = extrapolate(interpolate((b_UC_val, ), V2N_sol_bp[iik,iimtil,:], Gridded(Linear())),
  ```
- Line 1306: lat
  ```
  bpN_C_itp = extrapolate(interpolate((b_C_val[f_bCind:end], ), repeat([sspace.b_grid[1]], length(b_C_
  ```
- Line 1307: lat
  ```
  kpN_C_itp = extrapolate(interpolate((b_C_val[f_bCind:end], ), kpN_sol_muk[iik,iimtil,f_bCind:end], G
  ```
- Line 1308: lat
  ```
  mpN_C_itp = extrapolate(interpolate((b_C_val[f_bCind:end], ), mpN_sol_muk[iik,iimtil,f_bCind:end], G
  ```
- Line 1309: lat
  ```
  lambda2N_C_itp = extrapolate(interpolate((b_C_val[f_bCind:end], ), lambda2N_sol_muk[iik,iimtil,f_bCi
  ```
- Line 1310: lat
  ```
  V2Nk_C_itp    = extrapolate(interpolate((b_C_val[f_bCind:end], ), V2Nk_sol_muk[iik,iimtil,f_bCind:en
  ```
- Line 1311: lat
  ```
  V2Nmtil_C_itp = extrapolate(interpolate((b_C_val[f_bCind:end], ), V2Nmtil_sol_muk[iik,iimtil,f_bCind
  ```
- Line 1312: lat
  ```
  V2Nb_C_itp    = extrapolate(interpolate((b_C_val[f_bCind:end], ), V2Nb_sol_muk[iik,iimtil,f_bCind:en
  ```
- Line 1313: lat
  ```
  V2N_C_itp     = extrapolate(interpolate((b_C_val[f_bCind:end], ), V2N_sol_muk[iik,iimtil,f_bCind:end
  ```
- Line 1364: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 1381: lat
  ```
  # Using interpolation of V2Nmtil, compute the values of lambda1N implied by d=0 to determine which c
  ```
- Line 1382: lat
  ```
  lambda1N_mtilitp= extrapolate(interpolate((sspace.mtil_grid, ), V2Nmtil_in[ik,:,ib,iz]/(pQ_t*sspace.
  ```
- Line 1383: lat, son
  ```
  # NOTE: FLAT extrapolation -- REASON: to not make inference about a potentially "unconstrained state
  ```
- Line 1394: lat
  ```
  # If all mtils on grid are constrained, don't "extrapolate" unconstrainedness (as discussed above) a
  ```
- Line 1402: lat
  ```
  # Otherwise interpolate mtil_crit linearly based on the two adjacent mtil grid points; note that bei
  ```
- Line 1419: lat
  ```
  # Interpolate for the exact value of mtil at this crossing
  ```
- Line 1440: lat
  ```
  V2N_itp     = extrapolate(interpolate((sspace.mtil_grid, ), V2N_in[ik,:,ib,iz], Gridded(Linear())), 
  ```
- Line 1468: lat
  ```
  V2Nk_itp    = extrapolate(interpolate((sspace.mtil_grid, ), V2Nk_in[ik,:,ib,iz], Gridded(Linear())),
  ```
- Line 1469: lat
  ```
  V2Nmtil_itp = extrapolate(interpolate((sspace.mtil_grid, ), V2Nmtil_in[ik,:,ib,iz], Gridded(Linear()
  ```
- Line 1470: lat
  ```
  V2Nb_itp    = extrapolate(interpolate((sspace.mtil_grid, ), V2Nb_in[ik,:,ib,iz], Gridded(Linear())),
  ```
- Line 1471: lat
  ```
  V2N_itp     = extrapolate(interpolate((sspace.mtil_grid, ), V2N_in[ik,:,ib,iz], Gridded(Linear())), 
  ```
- Line 1509: second
  ```
  # 1bii  - Second stage of A
  ```
- Line 1518: lat
  ```
  kpA3_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.btil_grid, sspace.z_grid), 
  ```
- Line 1519: lat
  ```
  bpA3_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.btil_grid, sspace.z_grid), 
  ```
- Line 1520: second
  ```
  # NOTE: Because the "second stage" m' policies can be extreme, can have NaN returned, but unless any
  ```
- Line 1530: lat
  ```
  kpA2_itp = extrapolate(interpolate((sspace.k_grid, sspace.atil_grid, sspace.z_grid), kpA2_imp, Gridd
  ```
- Line 1531: lat
  ```
  bpA2_itp = extrapolate(interpolate((sspace.k_grid, sspace.atil_grid, sspace.z_grid), bpA2_imp, Gridd
  ```
- Line 1532: lat
  ```
  mpA2_itp = extrapolate(interpolate((sspace.k_grid, sspace.atil_grid, sspace.z_grid), mpA_cur, Gridde
  ```
- Line 1543: lat
  ```
  kpA1_itp = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), kpA1_imp, Gridded(
  ```
- Line 1544: lat
  ```
  bpA1_itp = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), bpA1_imp, Gridded(
  ```
- Line 1545: lat
  ```
  mpA1_itp = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), mpA1_imp, Gridded(
  ```
- Line 1546: lat
  ```
  dA1_itp  = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), dA_cur, Gridded(Li
  ```
- Line 1547: lat
  ```
  V1Ak_itp = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), V1Ak_cur, Gridded(
  ```
- Line 1548: lat
  ```
  V1Aa_itp = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), V1Aa_cur, Gridded(
  ```
- Line 1549: lat
  ```
  V1A_itp  = extrapolate(interpolate((sspace.k_grid, sspace.a_grid, sspace.z_grid), V1A_cur, Gridded(L
  ```
- Line 1579: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 1651: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.b_grid), V0e_tp1[:,imp,:,iz], Gri
  ```
- Line 1664: lat
  ```
  # Having established which elements of the b'- and b-vectors to keep, construct them here to use for
  ```
- Line 1671: lat
  ```
  bpA_uc_itp      = extrapolate(interpolate((btilA_uc_val[i_btilA_NC_val], ), sspace.b_grid[i_btilA_NC
  ```
- Line 1672: lat
  ```
  lambda3A_uc_itp = extrapolate(interpolate((btilA_uc_val[i_btilA_NC_val], ), Rc_t*M_tp1*V0eb_tp1[ikp,
  ```
- Line 1674: lat
  ```
  V0em_uc_itp     = extrapolate(interpolate((btilA_uc_val[i_btilA_NC_val], ), V0em_tp1[ikp,imp,i_btilA
  ```
- Line 1675: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((btilA_uc_val[i_btilA_NC_val], ), V0e_tp1[ikp,imp,i_btilA_
  ```
- Line 1678: lat
  ```
  bpA_uc_itp      = extrapolate(interpolate((btilA_uc_val, ), sspace.b_grid, Gridded(Linear())), NaN)
  ```
- Line 1679: lat
  ```
  lambda3A_uc_itp = extrapolate(interpolate((btilA_uc_val, ), Rc_t*M_tp1*V0eb_tp1[ikp,imp,:,iz]/sspace
  ```
- Line 1681: lat
  ```
  V0em_uc_itp     = extrapolate(interpolate((btilA_uc_val, ), V0em_tp1[ikp,imp,:,iz], Gridded(Linear()
  ```
- Line 1682: lat
  ```
  V0e_uc_itp      = extrapolate(interpolate((btilA_uc_val, ), V0e_tp1[ikp,imp,:,iz], Gridded(Linear())
  ```
- Line 1699: lat
  ```
  bpA_Bc_itp      = extrapolate(interpolate((btilA_Bc_val, ), repeat([sspace.b_grid[1]], sspace.NchiB)
  ```
- Line 1700: lat
  ```
  lambda3A_Bc_itp = extrapolate(interpolate((btilA_Bc_val, ), Rc_t*(M_tp1*V0eb_tp1[ikp,imp,1,iz] .+ ss
  ```
- Line 1702: lat
  ```
  V0em_Bc_itp     = extrapolate(interpolate((btilA_Bc_val, ), repeat([V0em_tp1[ikp,imp,1,iz]], sspace.
  ```
- Line 1703: lat
  ```
  V0e_Bc_itp      = extrapolate(interpolate((btilA_Bc_val, ), repeat([V0e_tp1[ikp,imp,1,iz]], sspace.N
  ```
- Line 1737: lat
  ```
  bpA_Sc_itp      = extrapolate(interpolate((btilA_Sc_val, ), repeat([sspace.b_grid[sspace.Nb]], sspac
  ```
- Line 1738: lat
  ```
  lambda3A_Sc_itp = extrapolate(interpolate((btilA_Sc_val, ), Rc_t*(M_tp1*V0eb_tp1[ikp,imp,sspace.Nb,i
  ```
- Line 1740: lat
  ```
  V0em_Sc_itp     = extrapolate(interpolate((btilA_Sc_val, ), repeat([V0em_tp1[ikp,imp,sspace.Nb,iz]],
  ```
- Line 1741: lat
  ```
  V0e_Sc_itp      = extrapolate(interpolate((btilA_Sc_val, ), repeat([V0e_tp1[ikp,imp,sspace.Nb,iz]], 
  ```
- Line 1767: city
  ```
  # For simplicity, separately write out separately the coefs of the quadratic eqn for k
  ```
- Line 1815: lat
  ```
  V0e_itp_temp      = extrapolate(interpolate((sspace.k_grid, sspace.b_grid), V0e_tp1[:,imp,:,iz], Gri
  ```
- Line 1827: lat
  ```
  # Having established which elements of the k'- and k-vectors to keep, construct them here to use for
  ```
- Line 1834: lat
  ```
  kpA_itp = extrapolate(interpolate((kA_val[i_kA_NC_val], ), sspace.k_grid[i_kA_NC_val], Gridded(Linea
  ```
- Line 1835: lat
  ```
  bpA_itp = extrapolate(interpolate((kA_val[i_kA_NC_val], ), bpA_sol_btil_temp[i_kA_NC_val,ibtil], Gri
  ```
- Line 1836: lat
  ```
  lambda3A_itp = extrapolate(interpolate((kA_val[i_kA_NC_val], ), lambda3A_sol_btil_temp[i_kA_NC_val,i
  ```
- Line 1837: lat
  ```
  V0em3A_itp = extrapolate(interpolate((kA_val[i_kA_NC_val], ), V0em_sol_btil_temp[i_kA_NC_val,ibtil],
  ```
- Line 1838: lat
  ```
  V0e3A_itp  = extrapolate(interpolate((kA_val[i_kA_NC_val], ), V0e_sol_btil_temp[i_kA_NC_val,ibtil], 
  ```
- Line 1841: lat
  ```
  kpA_itp = extrapolate(interpolate((kA_val, ), sspace.k_grid, Gridded(Linear())), Line())
  ```
- Line 1842: lat
  ```
  bpA_itp = extrapolate(interpolate((kA_val, ), bpA_sol_btil_temp[:,ibtil], Gridded(Linear())), Line()
  ```
- Line 1843: lat
  ```
  lambda3A_itp = extrapolate(interpolate((kA_val, ), lambda3A_sol_btil_temp[:,ibtil], Gridded(Linear()
  ```
- Line 1844: lat
  ```
  V0em3A_itp = extrapolate(interpolate((kA_val, ), V0em_sol_btil_temp[:,ibtil], Gridded(Linear())), Li
  ```
- Line 1845: lat
  ```
  V0e3A_itp  = extrapolate(interpolate((kA_val, ), V0e_sol_btil_temp[:,ibtil], Gridded(Linear())), Lin
  ```
- Line 1867: second
  ```
  # Solve second stage of A
  ```
- Line 1870: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 1884: lat
  ```
  V3Ak_itp_k_temp     = extrapolate(interpolate((sspace.m_grid, sspace.btil_grid), V3Ak_in[ik,:,:,iz],
  ```
- Line 1885: lat
  ```
  V3Am_itp_k_temp     = extrapolate(interpolate((sspace.m_grid, sspace.btil_grid), V3Am_in[ik,:,:,iz],
  ```
- Line 1886: lat
  ```
  V3Abtil_itp_k_temp  = extrapolate(interpolate((sspace.m_grid, sspace.btil_grid), V3Abtil_in[ik,:,:,i
  ```
- Line 1887: lat
  ```
  V3A_itp_k_temp      = extrapolate(interpolate((sspace.m_grid, sspace.btil_grid), V3A_in[ik,:,:,iz], 
  ```
- Line 1917: lat
  ```
  # Interpolate for the exact value of mp at this crossing
  ```
- Line 1980: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 1991: lat
  ```
  # Using interpolation of V2Aatil, compute the values of lambda1A implied by d=0 to determine which c
  ```
- Line 1992: lat
  ```
  lambda1A_atilitp= extrapolate(interpolate((sspace.atil_grid, ), V2Aatil_in[ik,:,iz]/sspace.k_grid[ik
  ```
- Line 1993: lat, son
  ```
  # NOTE: FLAT extrapolation -- REASON: to not make inference about a potentially "unconstrained state
  ```
- Line 2003: lat
  ```
  # If all atils on grid are constrained, don't "extrapolate" unconstrainedness (as discussed above) a
  ```
- Line 2011: lat
  ```
  # Otherwise interpolate atil_crit linearly based on the two adjacent atil grid points; note that bei
  ```
- Line 2026: lat
  ```
  # Interpolate for the exact value of atil at this crossing
  ```
- Line 2033: lat
  ```
  V2A_itp     = extrapolate(interpolate((sspace.atil_grid, ), V2A_in[ik,:,iz], Gridded(Linear())), NaN
  ```
- Line 2054: lat
  ```
  V2Ak_itp    = extrapolate(interpolate((sspace.atil_grid, ), V2Ak_in[ik,:,iz], Gridded(Linear())), Na
  ```
- Line 2055: lat
  ```
  V2Aatil_itp = extrapolate(interpolate((sspace.atil_grid, ), V2Aatil_in[ik,:,iz], Gridded(Linear())),
  ```
- Line 2056: lat
  ```
  V2A_itp     = extrapolate(interpolate((sspace.atil_grid, ), V2A_in[ik,:,iz], Gridded(Linear())), NaN
  ```
- Line 2078: lon
  ```
  # Functions for solving firm problem along IRF
  ```
- Line 2098: lon
  ```
  # Solve the firm's problem along a transition path, given a collection of price-paths faced.
  ```
- Line 2103: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2110: lon
  ```
  # Collectors that will contain solution objects along path
  ```
- Line 2153: lat
  ```
  # Functions for firm-level simulations
  ```
- Line 2156: lat
  ```
  # Simulate N firms, taking as given a CONSTANT set of policy functions on (k,m,b,z)
  ```
- Line 2162: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2181: lat
  ```
  # Create interpolants of the policy functions. For general robustness, allow for linear extrapolatio
  ```
- Line 2182: lat
  ```
  kpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), kpN_
  ```
- Line 2183: lat
  ```
  mpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), mpN_
  ```
- Line 2184: lat
  ```
  bpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), bpN_
  ```
- Line 2185: lat
  ```
  dN_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), dN_p
  ```
- Line 2186: lat
  ```
  kpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), kpA_
  ```
- Line 2187: lat
  ```
  mpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), mpA_
  ```
- Line 2188: lat
  ```
  bpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), bpA_
  ```
- Line 2189: lat
  ```
  dA_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), dA_p
  ```
- Line 2190: lat
  ```
  Ba_prob_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid),
  ```
- Line 2192: lat
  ```
  Yn_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), Yn_p
  ```
- Line 2193: lat
  ```
  Y_itp   = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), Y_po
  ```
- Line 2194: lat
  ```
  n_itp   = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), n_po
  ```
- Line 2195: lat
  ```
  ACN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), ACN_
  ```
- Line 2196: lat
  ```
  ACA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), ACA_
  ```
- Line 2206: lat
  ```
  # Simulate
  ```
- Line 2222: lat
  ```
  # Check whether firm steps out of grid, so that extrapolation is used, and write a warning
  ```
- Line 2225: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses kp=$(round(k_col[ii,tt+1],digits=4)) > 
  ```
- Line 2227: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses kp=$(round(k_col[ii,tt+1],digits=4)) < 
  ```
- Line 2231: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses mp=$(round(m_col[ii,tt+1],digits=4)) > 
  ```
- Line 2233: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses mp=$(round(m_col[ii,tt+1],digits=4)) < 
  ```
- Line 2237: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses bp=$(round(b_col[ii,tt+1],digits=4)) > 
  ```
- Line 2252: lat
  ```
  # Analogously, simulate firms taking as given the IRF set of policy functions on (k,m,b,z), containe
  ```
- Line 2253: lat, lon
  ```
  # Note that the length of the simulated path is the length implied by zi_col. If it is longer than t
  ```
- Line 2257: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2289: lat
  ```
  # Simulate over each t, with t running in the "outer loop"
  ```
- Line 2291: lat
  ```
  # Create interpolants of the policy functions. For general robustness, allow for linear extrapolatio
  ```
- Line 2292: lat
  ```
  kpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), kpN_
  ```
- Line 2293: lat
  ```
  mpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), mpN_
  ```
- Line 2294: lat
  ```
  bpN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), bpN_
  ```
- Line 2295: lat
  ```
  dN_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), dN_p
  ```
- Line 2296: lat
  ```
  kpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), kpA_
  ```
- Line 2297: lat
  ```
  mpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), mpA_
  ```
- Line 2298: lat
  ```
  bpA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), bpA_
  ```
- Line 2299: lat
  ```
  dA_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), dA_p
  ```
- Line 2300: lat
  ```
  Ba_prob_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid),
  ```
- Line 2302: lat
  ```
  Yn_itp  = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), Yn_p
  ```
- Line 2303: lat
  ```
  Y_itp   = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), Y_po
  ```
- Line 2304: lat
  ```
  n_itp   = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), n_po
  ```
- Line 2305: lat
  ```
  ACN_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), ACN_
  ```
- Line 2306: lat
  ```
  ACA_itp = extrapolate(interpolate((sspace.k_grid, sspace.m_grid, sspace.b_grid, sspace.z_grid), ACA_
  ```
- Line 2324: lat
  ```
  # Check whether firm steps out of grid, so that extrapolation is used, and write a warning
  ```
- Line 2327: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses kp=$(round(k_col[ii,tt+1],digits=4)) > 
  ```
- Line 2329: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses kp=$(round(k_col[ii,tt+1],digits=4)) < 
  ```
- Line 2333: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses mp=$(round(m_col[ii,tt+1],digits=4)) > 
  ```
- Line 2335: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses mp=$(round(m_col[ii,tt+1],digits=4)) < 
  ```
- Line 2339: lat
  ```
  if printwarnings; manprintln("WARNING: Firm ii=$(ii) chooses bp=$(round(b_col[ii,tt+1],digits=4)) > 
  ```
- Line 2379: lat
  ```
  # Function for simulating a random sample in SS and IRF, given solutions fed in, and conducting stud
  ```
- Line 2385: lon
  ```
  zi_col_long, xi_cdf_col_long =  Array{Int64,2}(undef, Nfirms,T_long), zeros(Nfirms, T_long)
  ```
- Line 2393: lon
  ```
  xi_cdf_col_long = rand(Nfirms,T_long)
  ```
- Line 2395: lat
  ```
  # Simulate MChains
  ```
- Line 2397: lat
  ```
  zi_col[ii,:]   = simulate(mcz,T,init=zi_col_init[ii])
  ```
- Line 2398: lat, lon
  ```
  zi_col_long[ii,:]   = simulate(mcz,T_long,init=zi_col_init[ii])
  ```
- Line 2402: lon
  ```
  sim_IRFSS_anl(se_init, zi_col, xi_cdf_col, zi_col_long, xi_cdf_col_long, fsoln, irfsoln, ind_plot, p
  ```
- Line 2404: lat
  ```
  # Function for taking a sample in SS and IRF, simulating, given solutions fed in, and conducting stu
  ```
- Line 2408: lat
  ```
  # First, simulate paths in IRF and SS
  ```
- Line 2410: lon
  ```
  sim_paths_SS_long  = sim_firm_SS(se_init, zi_col_long, xi_cdf_col_long, fsoln, printwarnings, modl, 
  ```
- Line 2427: lon
  ```
  data.cheat_dm = data.cheat - mean(sim_paths_SS_long.m_path ./ (sim_paths_SS_long.m_path + sim_paths_
  ```
- Line 2443: name
  ```
  x_col_names = ["cheat","cheat_dm"]
  ```
- Line 2444: name
  ```
  nx = length(x_col_names)
  ```
- Line 2457: name
  ```
  for (ix, x_str_temp) in enumerate(x_col_names)
  ```
- Line 2466: lon
  ```
  # Plot cheat alongside data estimates
  ```
- Line 2562: name
  ```
  for (ix, x_str_temp) in enumerate(x_col_names[[1]])
  ```
- Line 2571: lon
  ```
  # Plot coefficient alongside data estimates
  ```
- Line 2591: lon
  ```
  # Repeat above for interest rate on long-term debt as the outcome variable
  ```
- Line 2624: name
  ```
  for (ix, x_str_temp) in enumerate(x_col_names[[1]])
  ```
- Line 2633: lon
  ```
  # Plot coefficient alongside data estimates
  ```
- Line 2663: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2675: lat
  ```
  # Solve firm's problem to be used for the Young (2010) histogram approach to approximating populatio
  ```
- Line 2717: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 2795: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2840: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 2897: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3003: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 3035: lat
  ```
  # Guess for price path -- if no other guess is fed as input, guess flat path at SS values
  ```
- Line 3063: lon
  ```
  # Collections that will contain aggregate equilibrium solution objects along path
  ```
- Line 3107: lat
  ```
  # NKPC implies t inflation
  ```
- Line 3184: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3224: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3268: lat
  ```
  # Given the updated outcoming wage path (relative to incoming wage path), update the markup path
  ```
- Line 3305: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 3337: lat
  ```
  # Guess for price path -- if no other guess is fed as input, guess flat path at SS values
  ```
- Line 3365: lon
  ```
  # Collections that will contain aggregate equilibrium solution objects along path
  ```
- Line 3409: lat
  ```
  # NKPC implies t inflation
  ```
- Line 3487: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3528: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3572: lat
  ```
  # Given the updated outcoming wage path (relative to incoming wage path), update the markup path
  ```
- Line 3614: name
  ```
  txtout_path = joinpath(opts.project_root, "interim_output", "model_interim_output", "savedout", modl
  ```
- Line 3645: lat
  ```
  # Initialize "steady state paths" for all price paths -- useful to refer to later when solving indiv
  ```
- Line 3655: lat
  ```
  # Not used for any calculations, but just create as placekeepers:
  ```
- Line 3691: lat
  ```
  # Interest rates + inflation + capital price channel: rm, rb, rf, Pi, Q
  ```
- Line 3722: lat
  ```
  # Interest rates + inflation channel: rm, rb, rf + Pi
  ```
- Line 3726: lat
  ```
  # Insert correct values for the rate of interest, inferring a nominal rb rate path that, given a fla
  ```
- Line 3729: lat
  ```
  # And keep other nominal rates and the inflation rate constant at steady state
  ```
- Line 3753: lat
  ```
  # Interest rates + inflation channel: rm, rb, rf + Pi
  ```
- Line 3757: lat
  ```
  # Insert correct values for the rate of interest, inferring a nominal rb rate path that, given a fla
  ```
- Line 3760: lat
  ```
  # And keep other nominal rates and the inflation rate constant at steady state
  ```
- Line 3804: lon
  ```
  # Collections that will contain aggregate equilibrium solution objects along path
  ```
- Line 3847: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 3891: lat
  ```
  # Define functions related to adjustment cost
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Julia/Julia_model_code/calib_functions.jl**

- Line 3: lat
  ```
  # Function for computing pairwise correlation of (x,y), given grids and distribution L
  ```
- Line 25: lat
  ```
  # Create cumulative distribution
  ```
- Line 107: lat
  ```
  # Compute investment autocorrelations and standard deviations by simulation
  ```
- Line 121: lat
  ```
  # Simulate actual MChains
  ```
- Line 123: lat
  ```
  zi_col[ii,:]   = simulate(mcz,Tsim,init=zi_col_init[ii])
  ```
- Line 125: lat
  ```
  # Simulate paths in SS
  ```
- Line 130: lat
  ```
  # Compute averages, autocorrelations and standard deviations
  ```
- Line 181: lat
  ```
  # Average issuance cost relative to issuance size vector
  ```
- Line 221: lat
  ```
  # Function to characterize moments across firms' life-cycle. Currently simply computing entrants' si
  ```
- Line 265: loc
  ```
  Le_loc = Le_in / sum(Le_in);
  ```
- Line 271: loc
  ```
  L_age_col[:,1] = Le_loc;
  ```
- Line 273: loc
  ```
  La_last = Le_loc;
  ```
- Line 338: name
  ```
  Plots.savefig(joinpath(opts.project_root, "output", "figures", "model_figures", "FigB1_" * modl.name
  ```
- Line 346: birth, second
  ```
  println(io, "In the quarter of birth, about $(round(Int,100*round(diss_1q,digits=1)))% of firms issu
  ```
- Line 406: lon
  ```
  # Collections that will contain aggregate equilibrium solution objects along path
  ```
- Line 456: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 481: block, loc
  ```
  # Make this "Qend" into a block matrix Gamma, analogously as in Tan (2020), but adjusting for my dif
  ```
- Line 558: lat
  ```
  # Simulate MChains
  ```
- Line 560: lat
  ```
  zi_col[ii,:]   = simulate(mcz,T,init=zi_col_init[ii])
  ```
- Line 563: lat
  ```
  # First, simulate paths in IRF and SS
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/MATLAB/LPAgg_main.m**

- Line 1: loc
  ```
  % Matlab code used for aggregate local projection estimation in the paper
  ```
- Line 7: name
  ```
  this_file = mfilename('fullpath');
  ```
- Line 29: lat
  ```
  % Load consumption deflator
  ```
- Line 30: son
  ```
  CDeflev_xls = xlsread(fullfile(PROJECT_ROOT, 'data', 'raw', 'LPAgg_data', 'DNDGRD3Q086SBEA.xlsx'),'Q
  ```
- Line 31: lat
  ```
  CDeflev = CDeflev_xls(88:276,2); % CDeflator level
  ```
- Line 33: lat
  ```
  % Load investment deflator(s)
  ```
- Line 34: lat
  ```
  CIDeflator_xls = xlsread(fullfile(PROJECT_ROOT, 'data', 'raw', 'LPAgg_data', 'A006RD3Q086SBEA.xlsx')
  ```
- Line 35: lat
  ```
  STRIDeflator_xls = xlsread(fullfile(PROJECT_ROOT, 'data', 'raw', 'LPAgg_data', 'A009RD3Q086SBEA.xlsx
  ```
- Line 36: lat
  ```
  EQIDeflator_xls = xlsread(fullfile(PROJECT_ROOT, 'data', 'raw', 'LPAgg_data', 'Y033RD3Q086SBEA.xlsx'
  ```
- Line 38: lat
  ```
  % ApplyDef = Deflator;
  ```
- Line 39: lat
  ```
  ApplyDef = (CDeflev_xls(88:276,2)./CDeflev_xls(87:275,2)); % Nondurable deflator growth
  ```
- Line 40: lat
  ```
  CIDeflator = (CIDeflator_xls(88:276,2)./CIDeflator_xls(87:275,2)) ./ ApplyDef;          % CIdeflator
  ```
- Line 41: lat
  ```
  STRIDeflator = (STRIDeflator_xls(88:276,2)./STRIDeflator_xls(87:275,2)) ./ ApplyDef;    % STRIdeflat
  ```
- Line 42: lat
  ```
  EQIDeflator = (EQIDeflator_xls(88:276,2)./EQIDeflator_xls(87:275,2)) ./ ApplyDef;       % EQIdeflato
  ```
- Line 64: loc
  ```
  %% Local projection: Including a linear time trend
  ```
- Line 69: lat
  ```
  Ymat=[Ymat 100*log(STRIDeflator(startdate1:end,:)) 100*log(EQIDeflator(startdate1:end,:)) 100*log(CI
  ```
- Line 75: loc
  ```
  % Run local projections
  ```
- Line 91: lat
  ```
  % Cumulative sum of I inflation path
  ```
- Line 105: lat
  ```
  % Also, bootstrap cumulative sum of I inflation path
  ```
- Line 134: lat
  ```
  set(groot, 'defaultAxesTickLabelInterpreter','latex');
  ```
- Line 135: lat
  ```
  set(groot, 'defaultLegendInterpreter','latex');
  ```
- Line 136: lat
  ```
  set(groot, 'defaultTextInterpreter','latex');
  ```
- Line 157: name
  ```
  figurename=['FigA15b_IRF_YI_LPAgg'];
  ```
- Line 158: name
  ```
  figure('Name',figurename,'NumberTitle','off','Position',[1 1 720 720]);
  ```
- Line 159: name
  ```
  l1=plot([0:1:(L-1)],IRFscaled(3,:),'b-','LineWidth',4.5, 'DisplayName', 'Investment');
  ```
- Line 163: name
  ```
  l2=plot([0:1:(L-1)],IRFscaled(1,:),'-.','LineWidth',4.5, 'Color', [0.48 0.68 0.19], 'DisplayName', '
  ```
- Line 173: name
  ```
  set(gca, 'FontName', 'arial','FontSize',25);
  ```
- Line 175: loc, location
  ```
  legend([l1 l2], 'Location', 'southwest','FontSize',25);
  ```
- Line 178: name
  ```
  % printpdf(gcf,['figs_out/' figurename])
  ```
- Line 179: name
  ```
  l4=plot([0:1:(L-1)],mod_scaler*IRF_mod_Y,'k-.','LineWidth',3, 'DisplayName', 'Output (Model)');
  ```
- Line 180: name
  ```
  l5=plot([0:1:(L-1)],mod_scaler*IRF_mod_I,'k:','LineWidth',3, 'DisplayName', 'Investment (Model)');
  ```
- Line 181: loc, location
  ```
  legend([l1 l2 l5 l4], 'Location', 'southwest','FontSize',25, 'NumColumns',2);
  ```
- Line 182: name
  ```
  outfile = fullfile(outdir, [figurename '_wmod']);
  ```
- Line 185: name
  ```
  figurename=['FigA15a_IRF_FedFunds_LPAgg'];
  ```
- Line 186: name
  ```
  figure('Name',figurename,'NumberTitle','off','Position',[1 1 720 720]);
  ```
- Line 197: name
  ```
  set(gca, 'FontName', 'arial','FontSize',25);
  ```
- Line 201: name
  ```
  % printpdf(gcf,['figs_out/' figurename])
  ```
- Line 203: lat, loc, location
  ```
  legend([l1 l2], {'Fed funds rate', '$r_{t+1}^{f}$ (Model)'}, 'Location', 'southwest','FontSize',25, 
  ```
- Line 204: name
  ```
  outfile = fullfile(outdir, [figurename '_wmod']);
  ```
- Line 207: lat, name
  ```
  figurename=['FigA15c_IRF_CIDeflatorCS_LPAgg'];
  ```
- Line 208: name
  ```
  figure('Name',figurename,'NumberTitle','off','Position',[1 1 720 720]);
  ```
- Line 219: name
  ```
  set(gca, 'FontName', 'arial','FontSize',25);
  ```
- Line 223: name
  ```
  % printpdf(gcf,['figs_out/' figurename])
  ```
- Line 225: lat, loc, location
  ```
  legend([l1 l2], {'$P_{t}^{I}$', '$Q_{t}$ (Model)'}, 'Location', 'southwest','FontSize',25, 'NumColum
  ```
- Line 226: name
  ```
  outfile = fullfile(outdir, [figurename '_wmod']);
  ```
- Line 229: lat, name
  ```
  figurename=['FigA15d_IRF_ALLIDeflatorCS_LPAgg'];
  ```
- Line 230: name
  ```
  figure('Name',figurename,'NumberTitle','off','Position',[1 1 720 720]);
  ```
- Line 244: name
  ```
  set(gca, 'FontName', 'arial','FontSize',25);
  ```
- Line 246: lat, loc, location
  ```
  legend([l1 l2], {'$P_{t}^{I}$ (Structures)', '$P_{t}^{I}$ (Equipment)'}, 'Location', 'southwest','Fo
  ```
- Line 249: name
  ```
  outfile = fullfile(outdir, [figurename '_wmod']);
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/MATLAB/printpdf.m**

- Line 1: name
  ```
  function [filename] =  printpdf(fig, name)
  ```
- Line 26: name
  ```
  print(fig,'-depsc2', name)
  ```
- Line 28: name
  ```
  % Return full file name
  ```
- Line 29: name
  ```
  filename = [name, '.eps'];
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/DD_work_par_localwrapper.py**

- Line 1: loc
  ```
  # Python script to iterate over "DD_work_run.py" and set up parallelized workflow locally
  ```
- Line 12: name
  ```
  except NameError:
  ```
- Line 59: name
  ```
  if __name__ == "__main__":
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/DD_work_run.py**

- Line 2: loc
  ```
  # Run in array in HPC, or in parallel locally, loading data piece corresponding to "slurmcounter"
  ```
- Line 14: name
  ```
  except NameError:
  ```
- Line 31: loc
  ```
  crsp_data.loc[mask_fc,'datedif'] = np.nan
  ```
- Line 36: loc
  ```
  crsp_data.loc[crsp_data.datedif > 7, 'break_ind'] = True
  ```
- Line 44: loc
  ```
  gvb_drop_list = gvb_counts.loc[gvb_counts.gvkey < 252, 'gvkey_b']
  ```
- Line 55: loc
  ```
  GS1_raw.loc[GS1_raw.r_f=='.' ,'r_f']       = np.nan
  ```
- Line 57: lat
  ```
  GS1_raw['r_f_int'] = GS1_raw.r_f.interpolate(method='linear', axis=0, limit=999, limit_direction='fo
  ```
- Line 60: lat
  ```
  # Only keep observations before latest GDPDef observation (2020-06-01)
  ```
- Line 66: lat
  ```
  # Load GDP Deflator (2015=100)
  ```
- Line 69: lat
  ```
  # Impose midpoint of quarter (add 45 days) to GDP Deflator measurement
  ```
- Line 71: lat
  ```
  # Generate daily series of GDP Deflator by interpolation
  ```
- Line 74: lat
  ```
  GDPDef_daily['GDPDef_int'] = GDPDef_daily.GDPDef.interpolate(method='linear', axis=0, limit=999, lim
  ```
- Line 79: lat
  ```
  # Use the daily GDP deflator data and daily Treasury rate to compute the implied daily real rate
  ```
- Line 87: lat
  ```
  # Deflate main variables
  ```
- Line 113: loc
  ```
  start_tclock = time.time()
  ```
- Line 114: name
  ```
  for gv_name in gv_list:
  ```
- Line 116: loc
  ```
  elapsed_tclock = time.time() - start_tclock
  ```
- Line 117: loc, name
  ```
  print("Computing DD for firm "+gv_name+", "+str(ncount)+"/"+str(ntot)+"="+str(round(ncount/ntot,3))+
  ```
- Line 118: name
  ```
  f_data = crsp_data[crsp_data.gvkey_b == gv_name]
  ```
- Line 119: lat
  ```
  # Interpolate debt and market value variables
  ```
- Line 120: lat
  ```
  f_data['DD_debt_int'] = f_data.DD_debt.interpolate(method='linear', axis=0, limit=9999, limit_direct
  ```
- Line 121: lat
  ```
  f_data['mkt_eqy_int'] = f_data.mkt_eqy.interpolate(method='linear', axis=0, limit=30, limit_directio
  ```
- Line 134: lat
  ```
  # Calculate historical equity return and volatility
  ```
- Line 152: loc
  ```
  if f_data.iloc[tt-1]['DD_debt_int'] > 1e-10:
  ```
- Line 154: loc
  ```
  Va_per = Va_out.iloc[(tt-win):tt].copy()
  ```
- Line 175: loc
  ```
  r_f     = (1 + f_data.iloc[t_sub]['rr_f'])**(1.0/365) - 1
  ```
- Line 176: loc
  ```
  Ve      = f_data.iloc[t_sub]['mkt_eqy_int']
  ```
- Line 177: loc
  ```
  debt_val= f_data.iloc[t_sub]['DD_debt_int']
  ```
- Line 178: loc
  ```
  sol     = scoptim.root(lambda xx: BS_Va_dev(xx, sigma_a, debt_val, Ve, r_f, T), Va_out.iloc[t_sub])
  ```
- Line 179: loc
  ```
  Va_out.iloc[t_sub] = sol['x'][0]
  ```
- Line 183: loc
  ```
  Va_per = Va_out.iloc[(tt-win):tt].copy()
  ```
- Line 190: loc
  ```
  DD_out[subset_timesteps] = (np.log(Va_out.iloc[subset_timesteps]/f_data.iloc[subset_timesteps]['DD_d
  ```
- Line 202: loc, name
  ```
  crsp_data.loc[crsp_data.gvkey_b == gv_name, 'Va'] = Va_out
  ```
- Line 203: loc, name
  ```
  crsp_data.loc[crsp_data.gvkey_b == gv_name, 'DD'] = DD_out
  ```
- Line 204: loc, name
  ```
  crsp_data.loc[crsp_data.gvkey_b == gv_name, 'PD'] = PD_out
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/FOFA_analysis_code.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 17: name
  ```
  data.rename(columns = {'Series Description':'Year'}, inplace = True)
  ```
- Line 31: name
  ```
  # Redefine variable names for brevity
  ```
- Line 57: loc
  ```
  base_liq_shares  = 100*liq_shares.loc[1990:2008,:].mean()
  ```
- Line 58: loc
  ```
  pre90_liq_shares = 100*liq_shares.loc[1985:1989,:].mean()
  ```
- Line 60: lat
  ```
  # Return base shares as latex table
  ```
- Line 75: name
  ```
  asset_name = asset_map.get(code, code)
  ```
- Line 76: name
  ```
  lines.append(f"        {asset_name} & {val:.1f}\\% \\\\")
  ```
- Line 77: lat
  ```
  latex_table = (
  ```
- Line 108: lat
  ```
  f.write(latex_table)
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/Generate_TabB2.py**

- Line 1: lat
  ```
  # Script to create the Latex code for Table B.2
  ```
- Line 9: name
  ```
  except NameError:
  ```
- Line 54: lat
  ```
  # Write LaTeX rows
  ```
- Line 56: zip
  ```
  for i, (label, dval, mval) in enumerate(zip(row_labels, data_vals, model_vals)):
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/Master_empirics_work.py**

- Line 8: name
  ```
  except NameError:
  ```
- Line 44: lat
  ```
  # Clean raw CRSP and split up into 10 subpieces to manage RAM overload later on
  ```
- Line 48: loc
  ```
  # Split the generated data further into 60 subpieces (to make it easier to parallelize, either local
  ```
- Line 50: lat
  ```
  # Run DD calculations
  ```
- Line 51: loc
  ```
  run_python(PROJECT_ROOT / "code" / "Python" / "DD_work_par_localwrapper.py")  # (Loops locally over 
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/Micro_reg_prep_code.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 17: lat
  ```
  start_data = 1986.00 # Because sffr starts in 1990, only use late data
  ```
- Line 22: name
  ```
  data = data.drop(['Unnamed: 0'],axis=1)
  ```
- Line 33: loc
  ```
  data.loc[data.fyr.isin([11,12,1]), 'dattr_ind'] = 0
  ```
- Line 34: loc
  ```
  data.loc[data.fyr.isin([2,3,4]),'dattr_ind'] = 1
  ```
- Line 35: loc
  ```
  data.loc[data.fyr.isin([5]),'dattr_ind'] = 2
  ```
- Line 36: loc
  ```
  data.loc[data.fyr.isin([6,7]),'dattr_ind'] = -2
  ```
- Line 37: loc
  ```
  data.loc[data.fyr.isin([8,9,10]),'dattr_ind'] = -1
  ```
- Line 47: name
  ```
  ffr_raw = pd.read_excel(PROJECT_ROOT / 'data/raw/FEDFUNDS_Q.xls', sheet_name='FRED Graph', header=10
  ```
- Line 60: name
  ```
  GDP_table = pd.read_excel(PROJECT_ROOT / 'data/raw/GDPr_Q_BEA.xls', sheet_name='Sheet0', header=5, u
  ```
- Line 61: loc
  ```
  GDPr_raw = pd.DataFrame(GDP_table.loc[1].iloc[2:].astype(float).to_numpy(), index=np.linspace(1947.2
  ```
- Line 65: lat
  ```
  # Load GVA Deflator (2009=100)
  ```
- Line 66: name
  ```
  GVADef_table1 = pd.read_excel(PROJECT_ROOT / 'data/raw/GVADEF_Q_BEA.xls', sheet_name='Sheet0', heade
  ```
- Line 67: loc
  ```
  GVADef_raw1 = pd.DataFrame(GVADef_table1.loc[3].iloc[2:].astype(float).to_numpy(), index=np.linspace
  ```
- Line 68: name
  ```
  GVADef_table2 = pd.read_excel(PROJECT_ROOT / 'data/raw/GVADEF_Q_BEA.xls', sheet_name='Sheet1', heade
  ```
- Line 69: loc
  ```
  GVADef_raw2 = pd.DataFrame(GVADef_table2.loc[3].iloc[2:].astype(float).to_numpy(), index=np.linspace
  ```
- Line 82: zip
  ```
  sffr_raw['difquartend'] = [(end-date).days for end,date in zip(sffr_raw.quartend,sffr_raw.index)]
  ```
- Line 93: loc
  ```
  sffr_q.loc[sffr_q.index[0], 'MPshockSign_wd'] = sffr_q['MPshockSign_curquart'].iloc[0]
  ```
- Line 95: name
  ```
  # Rename dates as convention above
  ```
- Line 124: loc
  ```
  data.loc[data['fqtr']==1, 'capxq'] = data.loc[data['fqtr']==1, 'capxy']
  ```
- Line 126: loc
  ```
  data.loc[mask_fc,'capxq'] = np.nan
  ```
- Line 170: lon
  ```
  # Drop data with missing and negative Long-Term Debt
  ```
- Line 171: lon
  ```
  print("Dropping {} firm-quarters with negative and {} with missing \'Long Term Debt -- Total\', out 
  ```
- Line 189: name
  ```
  ccm_data = ccm_data.rename(columns={'GVKEY': 'gvkey'}, inplace=False)
  ```
- Line 201: loc
  ```
  g['cusip'].transform(lambda s: '99' if pd.isnull(s).all() == True else s.loc[s.first_valid_index()])
  ```
- Line 202: loc
  ```
  data['cusip_filled'] = g['cusip'].transform(lambda s: '99' if pd.isnull(s).all() == True else s.loc[
  ```
- Line 207: name
  ```
  data    = data.merge(wsc_data[['NAME','cusip','incdate_num','fnddate_num']], left_on='cusip_filled',
  ```
- Line 210: name
  ```
  data = data.drop(['conm_ccm', 'NAME','cusip_wsc'],axis=1)
  ```
- Line 211: name
  ```
  # Rename CUSIP back
  ```
- Line 212: name
  ```
  data = data.rename(columns={'cusip_cs': 'cusip'}, inplace=False)
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/TB3_analysis_code.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 20: loc
  ```
  tb3_8589_avg = data.loc[(data["year"] >= 1985) & (data["year"] <= 1989), "TB3MS"].mean()
  ```
- Line 21: loc
  ```
  tb3_9008_avg = data.loc[(data["year"] >= 1990) & (data["year"] <= 2008), "TB3MS"].mean()
  ```
- Line 33: lat
  ```
  f"Counterfactual model inflation rate: {counterf_pi:.2f}\n"
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/cleaning/CRSP_cleaning_code.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 14: loc
  ```
  file_loc = PROJECT_ROOT / 'proprietary-data-not-for-publication/raw/CRSP_data_raw.csv'
  ```
- Line 26: loc
  ```
  data_temp = pd.read_csv(file_loc, nrows=nlines_i, skiprows=range(1, ii*nlines_i+1))
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/cleaning/CRSP_par_prep.py**

- Line 2: lat
  ```
  # Order by gvkey, and split into 60 files to be later read in parallelized procedure to compute dist
  ```
- Line 11: name
  ```
  except NameError:
  ```
- Line 48: loc
  ```
  gv_sel = gv_list.iloc[range(ii*nper,min((ii+1)*nper, Nf))]
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/cleaning/CS_cleaning_code.py**

- Line 11: name
  ```
  except NameError:
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/cleaning/CStoCRSP_merge.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 14: lat
  ```
  int_QtoD_ind = True # Indicator for later linearly interpolating quarterly balance sheet data to dai
  ```
- Line 16: lat
  ```
  # Parameters related to splitting CRSP data
  ```
- Line 26: name
  ```
  ccm_data = ccm_data.rename(columns={'GVKEY': 'gvkey'}, inplace=False)
  ```
- Line 39: name
  ```
  cstat_data  = cstat_data.drop(['Unnamed: 0'],axis=1)
  ```
- Line 76: lat
  ```
  # If interpolate later, align gvkey "properly"
  ```
- Line 79: lat
  ```
  # If don't interpolate, align all variables "properly"
  ```
- Line 87: loc
  ```
  crsp_data.loc[mask_dates,'merge_date'] = crsp_data.loc[mask_dates,'merge_date'] - pd.tseries.offsets
  ```
- Line 88: loc
  ```
  crsp_data.loc[mask_dates,'merge_date'] = crsp_data.loc[mask_dates,'merge_date'] - pd.tseries.offsets
  ```
- Line 90: lat
  ```
  # If interpolate later, align gvkey "properly"
  ```
- Line 93: lat
  ```
  # If don't interpolate, align all variables "properly"
  ```
- Line 96: name
  ```
  for varname in crsp_data.columns:
  ```
- Line 97: name
  ```
  if varname[-3:] == '_f2':
  ```
- Line 98: name
  ```
  crsp_data[varname[:-3]].fillna(crsp_data[varname], inplace = True)
  ```
- Line 99: name
  ```
  del crsp_data[varname]
  ```
- Line 106: loc
  ```
  crsp_data.loc[mask_dates,'merge_date'] = crsp_data.loc[mask_dates,'merge_date'] - 2*pd.tseries.offse
  ```
- Line 107: loc
  ```
  crsp_data.loc[mask_dates,'merge_date'] = crsp_data.loc[mask_dates,'merge_date'] + pd.tseries.offsets
  ```
- Line 109: lat
  ```
  # If interpolate later, align gvkey
  ```
- Line 112: lat
  ```
  # If don't interpolate, align all variables
  ```
- Line 115: name
  ```
  for varname in crsp_data.columns:
  ```
- Line 116: name
  ```
  if varname[-3:] == '_f3':
  ```
- Line 117: name
  ```
  crsp_data[varname[:-3]].fillna(crsp_data[varname], inplace = True)
  ```
- Line 118: name
  ```
  del crsp_data[varname]
  ```
- Line 125: name
  ```
  for varname in crsp_data.columns:
  ```
- Line 126: name
  ```
  if varname[-3:] == '_f4':
  ```
- Line 127: name
  ```
  crsp_data[varname[:-3]].fillna(crsp_data[varname], inplace = True)
  ```
- Line 128: name
  ```
  del crsp_data[varname]
  ```
- Line 131: lat
  ```
  # Computing variables for DD calculations
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/Python/cleaning/Wscope_compile_code.py**

- Line 10: name
  ```
  except NameError:
  ```
- Line 14: name
  ```
  Wscope_data_filename = PROJECT_ROOT / 'proprietary-data-not-for-publication/raw/Wscope_data_raw.xlsx
  ```
- Line 15: name
  ```
  data_raw = pd.read_excel(Wscope_data_filename, sheet_name='Sheet1', header=0)
  ```
- Line 18: name
  ```
  data_temp = pd.read_excel(Wscope_data_filename, sheet_name='Sheet'+str(ii), header=0)
  ```
- Line 21: name
  ```
  # Rename columns
  ```
- Line 22: name
  ```
  data_raw = data_raw.rename(columns={'WC05601': 'tick', 'WC06004': 'cusip', 'WC18272': 'fnddate', 'WC
  ```
- Line 27: loc
  ```
  data_raw.loc[data_raw.cusip_len==9-nn,'cusip'] = '0'*nn + data_raw.loc[data_raw.cusip_len==9-nn,'cus
  ```
- Line 29: loc
  ```
  data_raw.loc[(data_raw.cusip_len > 9) | (data_raw.cusip_len < 6),'cusip'] = np.nan
  ```
- Line 31: loc
  ```
  data_raw.loc[data_raw.incdate.isnull(),'incdate'] = 99 # To convert to integers, need to set missing
  ```
- Line 34: loc
  ```
  data_raw.loc[data_raw.incdate_len==6, 'incdate_str'] = data_raw.loc[data_raw.incdate_len==6, 'incdat
  ```
- Line 35: loc
  ```
  data_raw.loc[data_raw.incdate_len==4, 'incdate_str'] = data_raw.loc[data_raw.incdate_len==4, 'incdat
  ```
- Line 36: loc
  ```
  data_raw.loc[data_raw.incdate_len==2, 'incdate_str'] = np.nan # Missing
  ```
- Line 41: loc
  ```
  data_raw.loc[data_raw.fnddate.isnull(),'fnddate'] = 99 # To convert to integers, need to set missing
  ```
- Line 44: loc
  ```
  data_raw.loc[data_raw.fnddate_len==6, 'fnddate_str'] = data_raw.loc[data_raw.fnddate_len==6, 'fnddat
  ```
- Line 45: loc
  ```
  data_raw.loc[data_raw.fnddate_len==4, 'fnddate_str'] = data_raw.loc[data_raw.fnddate_len==4, 'fnddat
  ```
- Line 46: loc
  ```
  data_raw.loc[data_raw.fnddate_len==2, 'fnddate_str'] = np.nan # Missing
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/R/Micro_eventstudy_plottr.r**

- Line 10: name
  ```
  PROJECT_ROOT <- normalizePath(file.path(dirname(script_path), "..", ".."), winslash = "/")
  ```
- Line 36: name
  ```
  # Structure filenames and plotting
  ```
- Line 57: name
  ```
  # Structure filenames
  ```
- Line 93: son
  ```
  # Also, plot comparison with model -- on SEPARATE y-axes, "aligning zeros"
  ```
- Line 97: lon
  ```
  # Plot results for main variable ALONGSIDE MODEL
  ```
- Line 120: second
  ```
  axis(side=4, at=pretty(ylim_alt), cex.axis = font_multr*font_ax_val)  # Add the secondary y-axis on 
  ```
- Line 132: second
  ```
  axis(side=4, at=pretty(ylim_alt), cex.axis=font_multr*font_ax_val)  # Add the secondary y-axis on th
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/R/Micro_model_reg_code.r**

- Line 11: name
  ```
  PROJECT_ROOT <- normalizePath(file.path(dirname(script_path), "..", ".."), winslash = "/")
  ```
- Line 42: lat
  ```
  data$latq <- log(data$atq)
  ```
- Line 98: loc
  ```
  # Drop outliers of adjustment rates, for local regression analysis
  ```
- Line 208: lat
  ```
  # Calculate marginal R^2 for each predictor
  ```
- Line 238: lname, name
  ```
  colnames(mr2_data) <- c("Controls", names(diss_model_list))
  ```
- Line 240: name
  ```
  for (iimodel in names(diss_model_list)){
  ```
- Line 254: lat
  ```
  # Generate LaTeX code
  ```
- Line 255: lat
  ```
  mr2_latex_code <- xtable(mr2_data_pp, caption = "Marginal $R^{2}$ in debt issuance regressions, perc
  ```
- Line 258: lat
  ```
  r2_tab <- capture.output(print(mr2_latex_code,
  ```
- Line 263: lat
  ```
  sanitize.text.function = function(x) x))  # To preserve the LaTeX formatting in the data
  ```

**/replication-package/JPE_REPLICATION_FINAL/code/R/Micro_reg_code.r**

- Line 10: name
  ```
  PROJECT_ROOT <- normalizePath(file.path(dirname(script_path), "..", ".."), winslash = "/")
  ```
- Line 73: lat
  ```
  # Recompute real assets based on GVA deflator
  ```
- Line 77: lat
  ```
  data$latq <- log(data$atq)
  ```
- Line 78: lat
  ```
  data$latq_real <- log(data$atq_real)
  ```
- Line 81: lon
  ```
  # Short and long leverage
  ```
- Line 104: lat
  ```
  # Relative to lagged assets
  ```
- Line 157: lat
  ```
  # Compute cumulative sums of "k_stock"
  ```
- Line 162: lat
  ```
  # Compute cumulative sums of "k_stock"
  ```
- Line 241: lon
  ```
  # Export to csv, alongside cqtr_num
  ```
- Line 246: lon
  ```
  # Statement about aggregate relevance of long-term debt in Compustat overall (Footnote 3)
  ```
- Line 258: lon
  ```
  writeLines(paste("At least ", round(100 * (1 - med_stdebt_share), 2), "% of total Compustat debt is 
  ```
- Line 282: lon
  ```
  # NET CHANGE in long-term debt
  ```
- Line 303: lon
  ```
  # Check firms which exhibit issuance activity or have long-term debt outstanding
  ```
- Line 308: lon
  ```
  writeLines(paste(round(100 * LTborrowers_share, 2), "% of firms have at least some long-term debt ou
  ```
- Line 313: lon
  ```
  writeLines(paste("In Compustat, ", round(100 * mean(p_data$lev_rat >= 0.01), 1), "% firm-quarters ha
  ```
- Line 367: loc
  ```
  # Drop outliers of adjustment rates, for local regression analysis
  ```
- Line 447: lat
  ```
  # Compute autocorrelation
  ```
- Line 455: lat
  ```
  compute_correlation <- function(x, y) {
  ```
- Line 460: lat
  ```
  # Compute the correlation if there are complete pairs
  ```
- Line 472: lat
  ```
  acor_ndltisqat_WF_dist = as.data.frame(ddply(p_data, c("gvkey"), summarize, ndltisqat_rat = compute_
  ```
- Line 474: lat
  ```
  acor_kinvat_WF_dist = as.data.frame(ddply(p_data, c("gvkey"), summarize, kinvat_rat = compute_correl
  ```
- Line 476: lat
  ```
  acor_dltisqat_WF_dist = as.data.frame(ddply(p_data, c("gvkey"), summarize, dltisqat_rat = compute_co
  ```
- Line 478: lat
  ```
  acor_invat_WF_dist = as.data.frame(ddply(p_data, c("gvkey"), summarize, invat_rat = compute_correlat
  ```
- Line 487: lat
  ```
  # Put everything together in a LaTeX table
  ```
- Line 497: lat
  ```
  # Generate LaTeX code
  ```
- Line 498: lat, lon
  ```
  lumpy_mom_latex_code <- xtable(lumpy_mom_data, caption = "Moments on the lumpiness of gross and net 
  ```
- Line 501: lat
  ```
  lumpy_mom_tab <- capture.output(print(lumpy_mom_latex_code,
  ```
- Line 507: lat
  ```
  sanitize.text.function = function(x) x))  # To preserve the LaTeX formatting in the data
  ```
- Line 572: lat
  ```
  formla_diss_lead4_noFE <- formula("diss_ind_lead4 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(che
  ```
- Line 575: lat
  ```
  formla_diss_lead4_FE <- formula("diss_ind_lead4 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(cheat
  ```
- Line 578: lat
  ```
  formla_diss_lead1_noFE <- formula("diss_ind_lead1 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(che
  ```
- Line 581: lat
  ```
  formla_diss_lead1_FE <- formula("diss_ind_lead1 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(cheat
  ```
- Line 584: lat
  ```
  formla_diss_lead8_noFE <- formula("diss_ind_lead8 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(che
  ```
- Line 587: lat
  ```
  formla_diss_lead8_FE <- formula("diss_ind_lead8 ~ 1 + rlag(tobin_q,0) + rlag(lev_rat,0) + rlag(cheat
  ```
- Line 599: lat
  ```
  # And write into LaTeX
  ```
- Line 603: lon
  ```
  reg_tab <- capture.output(stargazer(diss_model_list, title=tab_title, covariate.labels=cov_labs, dep
  ```
- Line 612: lon
  ```
  writeLines(c(reg_tab[-length(reg_tab)], post_tabular), file.path(PROJECT_ROOT, "output", "tables", "
  ```
- Line 615: lat
  ```
  # Calculate marginal R^2 for each predictor
  ```
- Line 639: lat
  ```
  mr2_controls_list = c("tobin_q", "lev_rat", "cheat_rat", "diss_ind", "dylsaleq_real", "latq", "lk_st
  ```
- Line 644: lname, name
  ```
  colnames(mr2_data) <- c("Controls", names(diss_model_list))
  ```
- Line 646: name
  ```
  for (iimodel in names(diss_model_list)){
  ```
- Line 660: lat
  ```
  # Generate LaTeX code
  ```
- Line 661: lat
  ```
  mr2_latex_code <- xtable(mr2_data_pp, caption = "Marginal $R^{2}$ in debt issuance prediction regres
  ```
- Line 664: lat
  ```
  r2_tab <- capture.output(print(mr2_latex_code,
  ```
- Line 669: lat
  ```
  sanitize.text.function = function(x) x))  # To preserve the LaTeX formatting in the data
  ```
- Line 679: lon
  ```
  writeLines(c(r2_tab[-length(r2_tab)], post_tabular_r2), file.path(PROJECT_ROOT, "output", "tables", 
  ```
- Line 687: lat
  ```
  keep_vars <- c("rlag(tobin_q, 0)", "rlag(lev_rat, 0)", "rlag(cheat_rat, 0)", "rlag(dylk_stock, 1)", 
  ```
- Line 732: lon
  ```
  "\\label{tab_reg_debtforec_longshort}",
  ```
- Line 777: name
  ```
  write.csv(df_event_emp, file.path(PROJECT_ROOT, "interim_output", paste0("emp_dissreg_vals_ind(", in
  ```
- Line 799: loc
  ```
  # Prepare data for local projection analysis
  ```
- Line 805: lon
  ```
  # Set up these lines to do a quick run, while work in progress, skipping long_samp, and only running
  ```
- Line 806: lon
  ```
  samp_ind_list = c("long_samp", "base_samp")
  ```
- Line 809: lon
  ```
  # Create outer loop to deal with long sample (2016Q4) robustness check in LP analysis
  ```
- Line 812: lon
  ```
  if (samp_ind == "long_samp"){
  ```
- Line 813: lon
  ```
  print("Running LP robustness analysis on long (up to 2016Q4) sample.")
  ```
- Line 832: lname, name
  ```
  colnames(RR_sffr_q) <- c("cqtr_num", "RRsh")
  ```
- Line 833: name
  ```
  rownames(RR_sffr_q) <- RR_sffr_q$cqtr_num
  ```
- Line 940: lat
  ```
  # Correlations function
  ```
- Line 942: lat
  ```
  # Create tables for collecting correlations with size
  ```
- Line 943: lat
  ```
  cors_cheat_size <- ddply(p_data_capx_q, .(cqtr_num), corfunc, var1="cheat_rat", var2="latq_real")
  ```
- Line 947: lat
  ```
  cors_temp_size <- ddply(p_data_capx_q, .(cqtr_num), corfunc, var1=var_temp, var2="latq_real")
  ```
- Line 950: lat
  ```
  # Create tables for collecting correlations with liquidity
  ```
- Line 960: name
  ```
  sumstat_df <- data.frame(var_name = sumstat_selvars, Mean=NA, Median=NA, SD=NA, Observations=NA, cor
  ```
- Line 961: name
  ```
  rownames(sumstat_df) <- sumstat_df$var_name
  ```
- Line 974: lat
  ```
  # Automated, formatted output to latex
  ```
- Line 979: name
  ```
  # Rename columns
  ```
- Line 980: lname, name
  ```
  colnames(sumstat_out) <- c("Mean", "Median", "St. dev", "Obs", "cor($\\cdot$, Liq)", "cor($\\cdot$, 
  ```
- Line 981: name
  ```
  # Rename rows
  ```
- Line 982: name
  ```
  rownames(sumstat_out) <- c( "Liquidity", "Leverage", "Size", "$\\Delta_{3} \\log(\\text{Sales})$", "
  ```
- Line 984: name
  ```
  sumstat_chr <- data.frame(lapply(sumstat_out, as.character), check.names = FALSE, row.names = rownam
  ```
- Line 1009: loc
  ```
  # Clean lines for untargeted moments and group-specific issuance frequencies in the draft, on local 
  ```
- Line 1032: lat
  ```
  chelatq_cors_t <- ddply(p_data_capx_q[(as.double(as.character(p_data_capx_q$cqtr_num))>=1990.0) & (p
  ```
- Line 1033: lat
  ```
  Table_Untarg_row2 <- -weighted.mean(chelatq_cors_t$cor, cheat_count_t$cheat_rat, na.rm=TRUE) # minus
  ```
- Line 1069: lat
  ```
  p_data_capx_q["Hlatq_ind"] <- NA
  ```
- Line 1079: lat
  ```
  p_data_capx_q <- transform(p_data_capx_q, latq_tdm = latq-ave(latq, sic3_datacqtr, FUN=function(x) q
  ```
- Line 1080: lat
  ```
  p_data_capx_q[p_data_capx_q$latq_tdm >= 0 | is.na(p_data_capx_q$latq_tdm), "Hlatq_ind"] <- 1
  ```
- Line 1081: lat
  ```
  p_data_capx_q[p_data_capx_q$latq_tdm < 0 | is.na(p_data_capx_q$latq_tdm), "Hlatq_ind"] <- 0
  ```
- Line 1082: lat
  ```
  p_data_capx_q[is.na(p_data_capx_q$latq_tdm), "Hlatq_ind"] <- NA
  ```
- Line 1095: name
  ```
  group_spec_Dfreq <- matrix(round(c(HL_Dfreq, HH_Dfreq, LL_Dfreq, LH_Dfreq), 4), nrow = 2, byrow = TR
  ```
- Line 1098: lat
  ```
  Table_Untarg_row9 <- mean(p_data_capx_q[rlag(p_data_capx_q$Hlatq_ind,1)==0,"diss_ind"], na.rm=TRUE)
  ```
- Line 1099: lat
  ```
  Table_Untarg_row10 <- mean(p_data_capx_q[rlag(p_data_capx_q$Hlatq_ind,1)==1,"diss_ind"], na.rm=TRUE)
  ```
- Line 1108: loc
  ```
  # Locally, set plot features to better format LP figures
  ```
- Line 1126: lat
  ```
  cumsum_resp_ind = FALSE # Instead of cumulative difference, do cumulative sum of LHS variable
  ```
- Line 1168: name
  ```
  shock_name_ext <- "_JKpmt"
  ```
- Line 1170: name
  ```
  shock_name_ext <- "_RR"
  ```
- Line 1172: name
  ```
  shock_name_ext <- ""
  ```
- Line 1182: name
  ```
  # Set filename figure labels, for clarity
  ```
- Line 1242: name
  ```
  # Add correct string at end of filenames
  ```
- Line 1263: lon
  ```
  # Only one "main" selector_q, alongside cheat_rat
  ```
- Line 1267: lat
  ```
  controls_ct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lagc
  ```
- Line 1269: lat
  ```
  controls_ct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat_dm, l
  ```
- Line 1272: lat
  ```
  controls_ct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lagc
  ```
- Line 1282: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lag
  ```
- Line 1284: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lag
  ```
- Line 1286: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat_dm, 
  ```
- Line 1288: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lag
  ```
- Line 1291: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(cheat_rat, lag
  ```
- Line 1297: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(latq, lagc1_ca
  ```
- Line 1300: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), paste("(rlag(",selec
  ```
- Line 1306: lat
  ```
  # Cumulative sum (log) instead of cumulative difference
  ```
- Line 1341: name
  ```
  formla_capx_q <- formula(paste("diff_dep_var ~ rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q)
  ```
- Line 1346: loc
  ```
  # Locally create the base regression's controls
  ```
- Line 1349: lat
  ```
  p_data_capx_q_used$llatq  <- rlag(p_data_capx_q_used$latq, lagc1_capx_q + 0)
  ```
- Line 1356: lat
  ```
  est_feols <- feols(diff_dep_var ~ lche + llatq + ldylsale + llev + lsffr:lche + lffr:lche + ldlgdp:l
  ```
- Line 1361: lat
  ```
  est_feols <- feols(diff_dep_var ~ lche + llatq + ldylsale + llev + lDD + lstdebt_share + lsffr:lche 
  ```
- Line 1374: name
  ```
  betas_vec_q[ctr,1] <- reg_coefs_capx_q[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx
  ```
- Line 1375: name
  ```
  ci_vec_q[ctr,] <- confint(reg_capx_q, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_c
  ```
- Line 1422: name
  ```
  c_name <- 1
  ```
- Line 1424: name
  ```
  varname="cheat_rat_dm"
  ```
- Line 1426: name
  ```
  varname="cheat_rat"
  ```
- Line 1431: name
  ```
  # print(varname)
  ```
- Line 1439: name
  ```
  betas_extra[ctr,1] <- reg_coefs_extra[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx_
  ```
- Line 1440: name
  ```
  ci_extra[ctr,] <- confint(cur_mod, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_capx
  ```
- Line 1443: name
  ```
  pdf(file.path(PROJECT_ROOT, "output", "figures", "empirics_LP", paste0(fig_extra_lab, "_slevs_betas_
  ```
- Line 1467: son
  ```
  # Estimate LP-IV specifications, for comparison with model
  ```
- Line 1507: name
  ```
  # Set filename figure labels, for clarity
  ```
- Line 1540: name
  ```
  # Add correct string at end of filenames
  ```
- Line 1550: lat
  ```
  lagc1_capx_q=1 + lplus_capx_q # lag of later controls
  ```
- Line 1556: name
  ```
  ct_varname_list =c(selector_q)
  ```
- Line 1560: name
  ```
  for (varname in inst_controls_ct_capx_q_all){
  ```
- Line 1561: name
  ```
  eval(parse(text=paste("p_data_capx_q$",varname,"Xsffr<-rlag(p_data_capx_q$",varname,",",as.character
  ```
- Line 1562: name
  ```
  eval(parse(text=paste("p_data_capx_q$",varname,"Xdffr<-rlag(p_data_capx_q$",varname,",",as.character
  ```
- Line 1566: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), paste("(rlag(",selec
  ```
- Line 1593: name
  ```
  formla_capx_q <- formula(paste("diff_dep_var ~ ", paste(controls_nct_capx_q, collapse=" + "), "  | "
  ```
- Line 1625: name
  ```
  shock_name_ext = "" # Baseline shocks
  ```
- Line 1638: name
  ```
  # Filename and legend labels
  ```
- Line 1648: name
  ```
  # Filename and legend labels
  ```
- Line 1657: name
  ```
  # Filename and legend labels
  ```
- Line 1678: name
  ```
  # Add correct string at end of filenames
  ```
- Line 1697: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(young_ind, lag
  ```
- Line 1723: name
  ```
  formla_capx_q <- formula(paste("diff_dep_var ~ rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q)
  ```
- Line 1731: name
  ```
  betas_vec_q[ctr,1] <- reg_coefs_capx_q[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx
  ```
- Line 1732: name
  ```
  ci_vec_q[ctr,] <- confint(reg_capx_q, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_c
  ```
- Line 1734: name
  ```
  yb_vec_q[ctr,1] <- reg_coefs_capx_q[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:
  ```
- Line 1735: name
  ```
  yb_ci_vec_q[ctr,] <- confint(reg_capx_q, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag
  ```
- Line 1744: name
  ```
  lev_ind <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q,", l
  ```
- Line 1745: name
  ```
  yb_ind  <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q,", l
  ```
- Line 1782: name
  ```
  shock_name_ext = ""     # Baseline shocks
  ```
- Line 1824: lat
  ```
  dummy_mat <- model.matrix(formula(paste("latq ~ atq + as.numeric(as.character(gvkey)) +", selector_q
  ```
- Line 1836: name
  ```
  # Add correct string at end of filenames
  ```
- Line 1849: lat
  ```
  lagc1_capx_q=1 + lplus_capx_q # lag of later controls
  ```
- Line 1867: lat
  ```
  controls_nct_capx_q = c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(latq, lagc1_c
  ```
- Line 1869: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(latq, lagc1_ca
  ```
- Line 1895: name
  ```
  formla_capx_q <- formula(paste("diff_dep_var ~ rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q)
  ```
- Line 1905: name
  ```
  betas_vec_q[ctr,ii] <- reg_coefs_capx_q[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_cap
  ```
- Line 1906: name
  ```
  ci_vec_q[ctr,ii,] <- confint(reg_capx_q, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag
  ```
- Line 1908: name
  ```
  betas_vec_q[ctr,ii] <- reg_coefs_capx_q[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_cap
  ```
- Line 1909: name
  ```
  ci_vec_q[ctr,ii,] <- confint(reg_capx_q, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag
  ```
- Line 1952: name
  ```
  c_name <- 1
  ```
- Line 1953: name
  ```
  for (varname in c("q_cheat_rat")){
  ```
- Line 1954: name
  ```
  nqc <- n_q_extra_vec[c_name]
  ```
- Line 1965: name
  ```
  betas_extra[ctr,1] <- reg_coefs_extra[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx_
  ```
- Line 1966: name
  ```
  ci_extra[ctr,1,] <- confint(cur_mod, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_ca
  ```
- Line 1969: name
  ```
  if (varname == "q_cheat_rat") {
  ```
- Line 1970: name
  ```
  betas_extra[ctr,ii] <- reg_coefs_extra[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx
  ```
- Line 1971: name
  ```
  ci_extra[ctr,ii,] <- confint(cur_mod, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_c
  ```
- Line 1973: name
  ```
  betas_extra[ctr,ii] <- reg_coefs_extra[, "Estimate"][c(paste("rlag(sffr",shock_name_ext,", lag0_capx
  ```
- Line 1974: name
  ```
  ci_extra[ctr,ii,] <- confint(cur_mod, level=ci_lev_set)[c(paste("rlag(sffr",shock_name_ext,", lag0_c
  ```
- Line 1978: name
  ```
  pdf(file.path(PROJECT_ROOT, "output", "figures", "empirics_LP", paste0(fig_extra_lab, "_betas_vec_q(
  ```
- Line 2004: name
  ```
  if (varname == "q_cheat_rat"){
  ```
- Line 2014: name
  ```
  c_name <- c_name+1
  ```
- Line 2033: lat
  ```
  cumsum_resp_ind = FALSE # Instead of cumulative difference, do cumulative sum of LHS variable
  ```
- Line 2039: name
  ```
  shock_name_ext  = ""        # Use baseline shock
  ```
- Line 2078: name
  ```
  # Add correct string at end of filenames
  ```
- Line 2090: lat
  ```
  lagc1_capx_q=1 + lplus_capx_q # lag of later controls
  ```
- Line 2094: lname, name
  ```
  colnames(agg_diff_series) <- "agg_diff_dep_var"
  ```
- Line 2097: name
  ```
  rownames(agg_mdiffs) <- agg_mdiffs$cqtr_num
  ```
- Line 2111: lat
  ```
  controls_nct_capx_q =c(paste("rlag(",selector_q,", lagc1_capx_q + 0)", sep=""), "rlag(latq_tdm, lagc
  ```
- Line 2166: name
  ```
  lev_ind <- match(paste("rlag(sffr, lag0_capx_q:lag1_capx_q)", sep=""), row.names(VCV_cur))
  ```
- Line 2170: name
  ```
  yb_ind  <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q, as.
  ```
- Line 2172: name
  ```
  yb_ind  <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q, as.
  ```
- Line 2190: name
  ```
  lev_ind <- match(paste("rlag(sffr, lag0_capx_q:lag1_capx_q)", sep=""), row.names(VCV_cur))
  ```
- Line 2195: name
  ```
  yb_ind  <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q, as.
  ```
- Line 2197: name
  ```
  yb_ind  <- match(paste("rlag(sffr",shock_name_ext,", lag0_capx_q:lag1_capx_q):rlag(",selector_q, as.
  ```
- Line 2243: city
  ```
  alpha_set = 0.2 # Opacity level of see-through lines
  ```

**/replication-package/JPE_REPLICATION_FINAL/install_Julia_packages.jl**

- Line 5: lat, name
  ```
  Pkg.add(name="LaTeXStrings", version="1.3.1")
  ```
- Line 6: lat, name
  ```
  Pkg.add(name="Interpolations", version="0.15.1")
  ```
- Line 7: name
  ```
  Pkg.add(name="Roots", version="2.2.1")
  ```
- Line 8: name
  ```
  Pkg.add(name="QuantEcon", version="0.16.6")
  ```
- Line 9: name
  ```
  Pkg.add(name="CompEcon", version="0.4.0")
  ```
- Line 10: name
  ```
  Pkg.add(name="JLD", version="0.13.5")
  ```
- Line 11: name
  ```
  Pkg.add(name="CSV", version="0.10.14")
  ```
- Line 12: name
  ```
  Pkg.add(name="Distributions", version="0.25.45")
  ```
- Line 13: name
  ```
  Pkg.add(name="DataFrames", version="1.5.0")
  ```
- Line 14: name
  ```
  Pkg.add(name="GLM", version="1.9.0")
  ```
- Line 15: name
  ```
  Pkg.add(name="Binscatters", version="0.4.0")
  ```
- Line 16: name
  ```
  Pkg.add(name="Plots", version="1.40.8")
  ```
- Line 17: name
  ```
  Pkg.add(name="AverageShiftedHistograms", version="0.8.9")
  ```

